LED RASTER DESIGNER - VERSION HISTORY
=====================================

v0.8.0 - May 4, 2026
----------------------------
MULTI-CANVAS RELEASE. Projects can now contain multiple independent
canvases (think Resolume Screens). Each canvas has its own raster size,
its own workspace position, its own perspective, and exports as its own
file (or its own page in PDF / its own Screen in Resolume XML).

Highlights:
- Multi-canvas project model (format_version: "0.8"). Auto-migrator
  upgrades v0.7 projects on load and emits a one-time toast asking the
  user to save in the new format. v0.7 builds opening a v0.8 file get a
  clean "format newer than supported" error.
- Canvas list in the right sidebar with per-canvas + Add, color swatch,
  rename, delete, duplicate, drag-reorder, and visibility toggle.
- Per-canvas raster (Pixel Map and Show Look). Toolbar Raster: WxH
  reads/writes the active canvas. Each canvas's layers render relative
  to its own origin.
- Per-canvas perspective (Front/Back) for Data Flow + Power. Mirror axis
  is the workspace bounding box so multi-canvas Back view stays on
  screen and Fit-to-View works correctly.
- Drag layers between canvases (workspace + sidebar). Snap to (0,0) on
  drop. Cmd/Alt+drag to duplicate.
- Drag canvases by their dashed outline. Magnetic snap to neighbor
  edges (abut + align). Default gap between auto-placed new canvases is
  zero (was 50 px).
- Hidden canvases excluded from sidebar Totals, capacity / power tallies,
  and exports. Sidebar layer rows dim with the eye toggle remaining
  bright for re-enable.
- Per-canvas + project Totals panels in the Data Flow and Power tabs.
- Text-layer dynamic info gains a Scope dropdown: "This Canvas",
  "All Canvases", or "Both" for the parent canvas's totals + project
  totals.
- Multi-canvas export: PNG (one file per canvas x view), PSD (per
  canvas+view, layer list filtered to that canvas), PDF (multi-page,
  one page per canvas x view, headed with the canvas name), and
  Resolume XML (one <Screen> per canvas, named after the canvas, with
  its own OutputDeviceVirtual sized to that canvas's raster).
- New screens added to a canvas inherit hardware settings (processor,
  voltage, amperage, panel size, panel watts) from the most recently
  added screen in that same canvas, so each canvas behaves like its own
  preset bucket.
- "+ Add Canvas" honors a Default Canvas Size preference.

Bug fixes the multi-canvas work uncovered:
- Undo now reverts exactly one action. Previously every v0.8 mutation
  (drag, canvas CRUD, cross-canvas move) called saveState BEFORE the
  mutation, so one Cmd+Z silently reverted two actions. Every v0.8
  saveState now runs AFTER the mutation, restoring v0.7 behaviour.
- 0-byte JSON saves on cloud-synced folders fixed. Skip Chrome's
  showSaveFilePicker on localhost (Chrome rejects createWritable on
  Nextcloud / iCloud / Dropbox paths) and route through the native
  server-side save dialog instead. Same fix applied to multi-file
  exports.
- "100%" zoom now means 1 raster pixel = 1 device pixel. Previously
  100% on Retina rendered at 2x the actual pixel pitch.
- Settings sidebar now refreshes after a cross-canvas drag's snap so
  X/Y inputs match the new (0,0) without a deselect/reselect.
- Custom port / power circuit assignment now rejects panels already
  wired to a different port or circuit (toast names the conflict).
- Text layer text now clips to its own box, no more spillover into
  neighboring canvases.
- Click-on-overlay (text/image floating on top of a screen) now selects
  the overlay instead of starting a panel-select on the hidden screen
  underneath.
- Em dash characters removed everywhere (UI text, comments, docs).

v0.7.7.4 - May 1, 2026
----------------------------
- FIX: Show Look / Data / Power renders dropped pieces of layers whose
  show offset shifted the layer past where the active raster started in
  processor coords. Specifically: panels, layer labels, screen names,
  cabinet ID numbers, data-flow arrows, power-circuit arrows, layer
  offset labels, and circle-with-X test patterns all clipped against
  raster bounds in *local* (per-layer translated) space instead of
  *screen* space, so the right side of show-shifted layers got cut off
  whenever the raster was narrower than the layer's processor right
  edge. Symptom: shrink the show raster below a layer's processor
  width, then grow it back, half the layer's panels and all its
  labels stayed missing in Show Look / Data / Power even though the
  layer was visually inside the new raster bounds.
  Root cause: each renderer had its own
    ctx.rect(0, 0, rasterWidth, rasterHeight); ctx.clip();
  inside the per-layer ctx.translate(dx, dy) block. Centralised the
  pattern into a translate-aware _clipToActiveRaster() helper that
  compensates for the active per-layer translate so the clip always
  lands on the screen-space raster boundary.
- FIX: "CANNOT FIT COMPLETE COLUMN" / capacity-error red overlays in
  Power and Data Flow rendered at the layer's pixel-map position even
  when the layer was moved in Show Look. Both overlays now use
  getLayerBoundsInActiveView so the badge follows the layer's show
  position.

v0.7.7.3 - May 1, 2026
----------------------------
- FIX: Panel catalog refresh failed in the macOS bundled app with
  "Couldn't reach GitHub". Root cause: Python's stdlib ssl module
  inside the PyInstaller .app couldn't find the system CA cert
  store, so every HTTPS handshake to raw.githubusercontent.com
  failed. We were already bundling certifi for this exact reason
  but never actually pointing urllib at it. Now the refresh route
  builds an SSL context with cafile=certifi.where() so verification
  succeeds in both dev and bundled builds.
- UX: Refresh button now properly suppresses spam clicks (previously
  9 stacked fetches were possible while it said "Refreshing…"),
  has a hard 15s client-side AbortController timeout so a hung
  request can't pin the UI, and surfaces the underlying server
  error in the failure toast instead of just "couldn't reach".
- LOGS: Server now logs panel_catalog_refresh_start/done/error so
  failures can be diagnosed without guessing whether the request
  reached Flask.
- INFRA: Release workflow auto-publishes tagged releases instead of
  leaving them as drafts (was the reason /releases/latest pointed
  at v0.7.6.4 even though .7.0/.1/.2 had been built).
- DELETE: Removed broken v0.7.7.2 release from GitHub Releases
  (catalog refresh never worked on macOS in that build).

v0.7.7.2 - May 1, 2026
----------------------------
- FEATURE: Panel catalog can now be refreshed live from GitHub without
  reinstalling the app. The Add Screen modal has a "↻ Refresh" button
  in the Panel Catalog header that pulls the latest panel_catalog.json
  from the repo. The fetch is proxied through the Flask backend so it
  works regardless of browser CORS or corporate firewall quirks.
  A small source tag next to the button shows whether the catalog is
  "Bundled" (shipping default) or "Updated <date>" (refreshed).
  On boot, the app does a silent background check against upstream;
  if a newer catalog exists, the source tag turns into a blue
  "📦 Update available · click to apply" pill that swaps in the new
  data with a single click. Refreshed catalog persists in localStorage
  so it survives reloads, per browser, not per project.
- FIX: Panel correction / new-panel submissions were silently dropping.
  The "Submit a correction or add a panel" link opens a pre-filled
  GitHub new-issue page, but users were closing the tab without
  clicking the green "Submit new issue" button on GitHub itself,
  so the submission never reached the repo. Added an explicit confirm
  dialog before opening the tab and an inline note under the link
  spelling out the extra step. Also created the missing
  "spec-correction" and "add-panel" GitHub labels server-side so
  future submissions are filterable on the issues page.

v0.7.7.1 - May 1, 2026
----------------------------
- FEATURE: Favorites for the Add Screen picker. Click the heart icon on
  any row in the Panel Catalog to add it to the left column alongside
  your saved presets. Hearted panels stay there forever (per user, not
  per project) so you don't have to scroll the catalog every time you
  add the same panel. Drag any row in the left column to reorder,
  favorites and presets mix freely. Click the filled heart to remove a
  favorite. Default is always pinned at the top.

v0.7.7.0 - May 1, 2026
----------------------------
- FEATURE: Front/Back view perspective toggle for Data Flow and Power
  tabs (Issue #31). Each tab has its own independent Front/Back toggle
  in the sidebar. Back view horizontally mirrors the canvas geometry
  (so wiring matches what you actually see standing behind the wall)
  while keeping every text label readable. A "BACK VIEW" badge shows
  in the top-right corner of the canvas whenever Back is active. On
  export, the filename suffix for that view auto-appends "_back" when
  in Back perspective so front/back artwork doesn't collide.

v0.7.6.4 - May 1, 2026
----------------------------
- FIX: Collapsing a sidebar left a black strip on the canvas where the
  sidebar used to be. The canvas wrapper grew with the freed space but
  the canvas element kept its pre-collapse pixel dimensions because
  setupCanvas() ran during the CSS width transition (before the wrapper
  reached its final size). Canvas resize now fires at multiple points
  during and after the transition so the canvas always fills the full
  available width.

v0.7.6.3 - May 1, 2026
----------------------------
- FEATURE: Collapsible side panels. A small chevron tab on the inner
  edge of each sidebar collapses/expands the panel independently. The
  toggle anchors to the sidebar's actual edge via ResizeObserver so it
  stays in place at any window/sidebar size. Collapsed state persists
  in localStorage per side.
- FIX: Editing or dragging a layer's Pixel Map offset now mirrors the
  change to the Show Look offset while the two are still equal
  (linked). Without this, moving a layer in Pixel Map left Show Look /
  Data / Power rendering at the old position. Once you set a different
  Show Look offset, the two stay independent, Pixel Map edits no
  longer touch Show Look.
- FIX: Screen name labels and other per-layer labels were rendered
  off-center in Show Look / Data / Power because the per-layer ctx
  translate AND the bounds-calculation both added the show-offset,
  double-shifting label positions. Bounds returned by getLayerBounds
  are now in raw processor coords; new getLayerBoundsInActiveView
  helper is used for the few callers that operate outside the
  per-layer translate (selection bounding box, hit-test, magnetic
  snap, screen-name drag, zoom-to-fit).
- FIX: "Reset to Pixel Map Position" appeared to put screens in the
  wrong spot, same root cause as the label bug above. Now resets
  cleanly with screens visually matching their pixel-map positions.

v0.7.6.2 - April 30, 2026
----------------------------
- FIX: Opening a project file (recent or via the file picker) didn't
  sync the renderer's pixel/show raster backing fields, so Show Look
  showed the wrong raster size after a file open even though the
  project's data was correct. Added a single syncRasterFromProject()
  helper that all project-load paths now call (initial socket connect,
  HTTP /api/project, file-picker open, recent files, new project) so
  Show Look picks up the file's saved show raster, falling back to
  the pixel raster when the file pre-dates the Show Look feature.

v0.7.6.1 - April 30, 2026
----------------------------
- FIX: Show Look raster now tracks the Pixel Map raster by default.
  Changing the Pixel Map raster size also updates the Show Look raster
  while they're equal (which is the case for new projects and any
  project where Show Look hasn't been explicitly resized). Once you
  set a different raster size in Show Look, the two stay independent.

v0.7.6.0 - April 30, 2026
----------------------------
- FEATURE: Show Look tab. New tab between Cabinet ID and Data that lets
  you rearrange screen layers to match the real-world stage layout
  without affecting the processor's expected layout.
    - Pixel Map and Cabinet ID continue to use each layer's processor
      position (offset_x / offset_y), what the processor sends out.
    - Show Look has its own per-layer position (showOffsetX / Y), which
      also drives the Data and Power views so the wiring/power maps
      match how the show is actually built on stage.
    - Pixel Map and Show Look each have an independent raster size.
      The toolbar Raster width/height edits the active view's raster:
      pixel-map / cabinet-id edits the processor raster, show-look /
      data / power edits the show raster.
    - Shift-drag a screen on the canvas in Show Look (or Pixel Map) to
      reposition it. Pixel Map drag writes to the processor position;
      Show Look drag writes to the show position. The two can differ.
    - Sidebar in Show Look exposes Show Offset X / Y inputs and a
      "Reset to Pixel Map Position" button to re-sync.
    - Show Look added to the export dialog with its own filename suffix.
    - Existing projects open with show position = pixel position and
      show raster = pixel raster, so nothing visually changes until you
      start moving things in Show Look.
- FIX: Hidden ("blank") panels in a Pixel Map drag-selection now render
  a stronger blue tint than visible panels so you can clearly see which
  blank cells are part of the selection. Without this, the normal
  35%-alpha overlay was barely visible against the dark background of
  an empty hidden cell.

v0.7.5.3 - April 30, 2026
----------------------------
- FIX: Hidden ("blank") panels could not be selected via drag-select,
  plain click, or right-click in Pixel Map view, so there was no way to
  bulk-restore them via the sidebar buttons. Hidden panels can now be
  selected for bulk Restore. (Alt+Shift+click for half-tile still skips
  hidden panels, half-tiling a hidden cell would have no visible effect.)

v0.7.5.2 - April 30, 2026
----------------------------
- Internal: tests and lint cleanup for the v0.7.5 half-tile rewrite.
  No user-facing behavior changes.
    - Tests updated for the per-panel halfTile model: legacy
      halfFirstColumn / halfLastColumn / halfFirstRow / halfLastRow
      flags now migrate to per-panel halfTile state on first build,
      and a corner cell affected by both row+column legacy flags
      becomes half-height (row flag wins) instead of the old
      "quarter cabinet" combination.
    - New tests cover (row, col)-keyed panel state preservation
      across grid resizes (the v0.7.5.0 fix that stops blanks from
      scattering when columns/rows change).
    - Removed dead code surfaced by JS lint: getUnitVisibleRange
      (replaced by pixel-extent calc in calcBoundingRectLoad),
      ILLEGAL_FILENAME_CHARS (regex inlined), and a few unused
      local bindings.

v0.7.5.1 - April 30, 2026
----------------------------
- UX: "Set Half-tile (auto)" on a multi-selection now picks one
  direction (width vs. height) by majority vote across the selected
  panels, then applies that direction uniformly. Previously each panel
  resolved its own direction, which could split a selected row into
  some half-height and some half-width panels when one of them was an
  interior or corner cell.

v0.7.5.0 - April 30, 2026
----------------------------
- FEATURE: Per-panel half-tile editing replaces the four screen-level
  "Half First/Last Column/Row" checkboxes. Mark any individual panel as
  half-width or half-height for non-rectangular walls and stepped layouts.
    - Drag-select panels in Pixel Map view → sidebar shows action
      buttons: Set Blank, Restore, Set Half-tile (auto), Force
      Half-Width, Force Half-Height, Restore Full. Selection count
      badge in the canvas corner.
    - Right-click a panel (or selection) → context menu with the same
      actions.
    - Alt + click toggles blank on a single panel (existing), when a
      multi-selection is active, applies to the entire selection.
    - Alt + Shift + click toggles half-tile (auto direction) on a single
      panel, when a multi-selection is active, applies to the entire
      selection. Auto direction picks 'height' for top/bottom-edge panels
      and 'width' for left/right-edge panels.
    - Plain click on a panel resets selection to that single panel;
      Cmd/Ctrl + click adds/removes from selection. Esc clears the
      selection.
    - Half-tile cabinets render anchored to their visible neighbor so
      the missing half sits on the wall's outer edge (matching how
      half cabinets are physically installed at wall edges).
    - Math: half-tiles count as 0.5 panels for data/port mapping,
      0.65 panels for power/weight (existing helpers already do this
      via pixel area + load factor).
    - Legacy halfFirstColumn / halfLastColumn / halfFirstRow / halfLastRow
      flags are migrated to per-panel halfTile values on first load,
      then dropped from the schema. Existing projects keep their look.
- FIX: NovaStar 1G/Armor port load calculation now uses the actual
  pixel bounding rect (panel x/y/width/height) instead of the cabinet-
  cell shortcut. Half-tile cabinets correctly contribute half their
  full-cell pixel footprint to the port reservation rectangle.
- FIX: Pre-existing bug, hidden ("blank") panels in the middle of a
  wall would scatter to other positions when columns or rows were
  resized. Panel state was preserved by sequential id, so the same id
  pointed to a different (row, col) cell after a grid resize. State
  is now keyed by (row, col) so blanks and half-tiles stay anchored
  to their original positions across resizes (within the new bounds).

v0.7.4.35 - April 29, 2026
----------------------------
- FIX: Ports Required count and the port-rename editor showed too few
  ports when using custom data flow paths, e.g. 8 custom paths drawn
  but the count stayed at the auto-pattern's 5. The client-computed
  _portsRequired and _autoPortsRequired fields were being clobbered by
  the server's stale echo on every PUT round-trip (server's whitelist
  ignores these underscore-prefixed derived fields, but the response
  body still carried the old values). Both fields are now preserved
  client-side across the round-trip, and the toggle handler recomputes
  the count before sending so the freshly computed value is what gets
  preserved.

v0.7.4.34 - April 29, 2026
----------------------------
- UX: Project name field now shows an inline warning ("Note: / will be
  replaced with _ in exported filenames.") when it contains characters
  the OS rejects in filenames (\ / : * ? " < > |). Gives the user a
  chance to rename before exporting; if they don't, v0.7.4.33's export-
  time sanitization still produces a valid filename.

v0.7.4.33 - April 29, 2026
----------------------------
- FIX: Multi-file PNG/PSD export failed with "Failed to execute
  'getFileHandle' on 'FileSystemDirectoryHandle': Name is not allowed."
  when the project name (used as the filename prefix) contained a forward
  slash, backslash, colon, or other characters the OS rejects in
  filenames. Common case: project named "Foo/Bar" produced filenames like
  "Foo/Bar_pixel-map.png", which the File System Access API refuses.
  Filenames are now sanitized, illegal characters are replaced with "_"
  before being passed to the picker / directory handle. Single-file
  exports (PDF, JSON save, etc.) get the same treatment.

v0.7.4.32 - April 29, 2026
----------------------------
- FIX: Power circuit label templates other than the default "S1-#" did not
  auto-increment the soca number after every 6 circuits. With "S1-#" the
  labels correctly wrapped S1-1..S1-6, S2-1..S2-6, etc., but typing any
  other starting number ("S2-#", "MULTI3-#", etc.) fell back to a plain
  "#" → circuit-number substitution, so circuits 7+ rolled past 6 instead
  of starting a new soca. The auto-increment now works for any template
  shaped <prefix><number><sep>#, "S2-#" produces S2-1..S2-6, S3-1..S3-6;
  "MULTI3-#" produces MULTI3-1..MULTI3-6, MULTI4-1..MULTI4-6.

v0.7.4.31 - April 27, 2026
----------------------------
- FIX: Deleting a layer and pressing Undo could revert port-label or
  circuit-label edits made on other screens since the previous undoable
  action. The inline rename inputs in the Data Flow and Power sidebars
  saved their edits to state but never pushed an undo snapshot, so Undo
  jumped past them to an older snapshot and reverted them along with the
  delete. Each port-label and circuit-label edit now records its own
  history entry, so Undo only steps back one edit at a time.

v0.7.4.30 - April 28, 2026
----------------------------
- FIX: In Data Flow and Power views, clicking a different screen left the
  port-rename / circuit-rename sidebar empty until you clicked a second
  time. selectLayer() updated the canvas and the right-hand inputs but
  never re-populated the left-side label editor; the editor only refreshed
  the next time something else nudged it. Now it's repainted in lockstep
  with the layer change.

v0.7.4.29 - April 28, 2026
----------------------------
- FIX: Toggling Offset TL/TR/BL/BR (or any other property checkbox) with
  multiple screens selected was clobbering each layer's "Labels Font Size"
  and "Info Label Size" to null whenever the selected layers had mixed
  values for those fields. The shared input shows blank for mixed values,
  parseInt('') returns NaN, and the NaN guard slipped past the null check
 , so every selected layer ended up with labelsFontSize=null. Switched
  both inputs to use the same readNumber() helper the other numeric
  fields in this code path already use, which correctly returns null for
  blank/NaN reads and skips the apply.
- INTERNAL: Bumped GitHub Actions to Node.js 24-compatible majors,
  upload-artifact v6→v7, download-artifact v6→v8, action-gh-release v2→v3
  (resolves the deprecation warning surfaced on PR #34).

v0.7.4.28 - April 25, 2026
----------------------------
- NEW: "Found bad specs? Submit a correction →" link in the panel
  catalog. Opens a pre-filled GitHub issue (zero-server) where users
  can drag a PDF spec sheet or back-of-panel photo into the comment
  to attach. Issues land at github.com/kman1898/LED-Raster-Designer/issues
  with the spec-correction label, including the panel reference, the
  current catalog values, the user's notes, and the app version.
- DATA: Leyard verified, CLI-1.9, CLI-2.6, CLO-3.9, CLM-6.9, CLA-1.9
  all match Planar/eanixter PDFs. TVF1.5 watts corrected from 180.4W
  → 140W (per official Planar TVF data sheet). Duplicate CLA1.5 +
  CLA1.9 entries deduped (kept hyphenated form to match spec sheets).
- DATA: Barco verified, X1.9, XT1.5 (renamed from "1.5XT"), X4, NX4
  cross-checked against barco.com PDFs. X4 watts corrected from 60.5W
  → 188.9W (factor-of-3 error from the original extraction). NX4
  watts corrected from 300.3W → 244W.
- Verified panel count now 92 (was 75).

v0.7.4.27 - April 25, 2026
----------------------------
- FIX: ⭐ verified marker now correctly recognizes ROE panels sourced
  from roevisual.com (brochures + PDFs) and authoritative dealer
  datasheets (ledwallcentral, xled.pro). Verified panel count jumped
  from ~28 to 75, primarily 50 ROE models (Carbon, Topaz, Vanish,
  Black Pearl/Onyx, Ruby, Graphite, etc.) plus existing Absen / INFiLED
  / UniLumin / Gloshine entries. Entries flagged as "estimated" or
  "derived" are still excluded.

v0.7.4.26 - April 25, 2026
----------------------------
- NEW: Add Screen modal redesigned. Picking a panel from the built-in
  catalog now happens in the same flow as picking a saved preset, two
  columns side by side. Search the catalog by name or manufacturer; pick
  one panel, click Add Screen, and the cabinet/pixel/weight/power values
  fill in automatically (grid size still comes from Preferences).
- NEW: ⭐ verified marker. Panels whose specs were cross-checked against
  the manufacturer's own published spec sheet/PDF show a star. Currently
  marks ~30 panels across Absen (JP, A3 Pro, A6T, VN), INFiLED (ART,
  AMT, DB), UniLumin (UGM), and Gloshine. Other catalog entries come from
  FidoLED's database, accurate but occasionally older than current specs.
- DATA: Power values for all 2,536 panels re-validated. The original
  extraction missed a 110V voltage selection in FidoLED for a chunk of
  the catalog, leaving panels with corrupt watts (e.g. JP8 Pro showed
  16.75W instead of the real ~480W). Re-ran the entire catalog through
  FidoLED's "Copy to Clipboard" output with 110V properly set, plus
  manually-applied PDF values for the JP series.
- DATA: 12 phantom panels removed (Mambo-* entries that don't actually
  exist in FidoLED's menus).
- INTERNAL: clipboard_extract.sh hardened, Escape-key recovery to
  prevent FidoLED popup wedge cascades, case-insensitive title verify,
  abort-on-cascade detection, skip-on-missing-power so we never overwrite
  good data with null. New post_batch.py + prep_no_source_batch.sh make
  the maintenance loop a one-liner per batch.
- INTERNAL: app.py accepts --port N to run alongside other Flask apps.

v0.7.4.25 - April 24, 2026
----------------------------
- NEW: Built-in Panel Catalog now covers 2,543 panels across 180
  manufacturers (up from ~1,400). Every panel in FidoLED's database is
  represented with width, height, pixel count, weight, and max power.
- INTERNAL: Added tools/panel_extractor for maintaining the catalog,
  a clipboard-based AppleScript pipeline that drives FidoLED's Calculate
  + Copy to Clipboard flow, parses the output, and merges into
  src/static/data/panel_catalog.json.

v0.7.4.24 - April 23, 2026
----------------------------
- NEW: Watts-per-Panel field accepts arithmetic expressions (+, -, *, /,
  parentheses). Type "200+50", "500*2", "1000/3", etc. The field resolves
  to the computed value on blur and flashes a red outline if the expression
  is invalid.
- NEW: Custom data/power flow badges (top-left of canvas) now show panel
  counts. A white "N on port" / "N on circuit" pill shows how many panels
  are already committed to the active port/circuit, and a yellow "+M
  selected" pill appears live while drag-selecting panels so you can see
  pending vs. locked-in counts at a glance.

v0.7.4.23 - April 22, 2026
----------------------------
- FIX: Preset values (bit depth, frame rate, processor type, flow pattern,
  data-flow colors/label sizes, port templates, screen-name offsets, info-label
  toggles, etc.) now persist server-side after applying a preset. Root cause:
  the server's PUT /api/layer/<id> endpoint had an incomplete whitelist that
  silently dropped these fields, so presets would appear to work until any
  subsequent server re-fetch (delete layer, project load) reset them.
- FIX: Preset sync, `addLayer` now pushes the preset-enriched layer to the
  server immediately after creation, so the server has complete state instead
  of only the structural fields sent via `/api/layer/add`.
- FIX: Preset serialization always includes `portMappingMode` and
  `randomDataColors` defaults so presets saved from older-format layers are
  still complete.
- NEW: Save-as-Preset modal shows a list of existing presets; click any to
  pre-fill the name field. The Save button label switches to "Overwrite"
  when the typed name matches an existing preset.

v0.7.4.22 - April 22, 2026
----------------------------
- NEW: Logs viewer gains Since / Until time filters. Enter a relative time
  ("10 min ago", "2h ago", "30s ago", "1d ago") or an absolute timestamp
  ("YYYY-MM-DD HH:MM:SS") to narrow the visible entries; leave both blank
  to show everything. Invalid input is highlighted; a Clear Filter button
  wipes both fields.

v0.7.4.21 - April 22, 2026
----------------------------
- FIX: Info labels (Data tab's "X Mains, Y Backups | Z Ports" and Power tab's
  "X Multi, Y Circuits | A 1φ / B 3φ") now update immediately when you change
  a setting on multiple selected screens. Previously the cached port/circuit
  counts were only refreshed for the currently selected layer's side panel,
  so other selected screens kept stale labels until clicked.
- NEW: Help → Show Logs… Opens an in-app viewer for the application log.
  Features: adjustable line count (250/500/1000/5000/20000), auto-refresh
  every 2s, wrap-lines toggle, Copy button, Open Folder (reveals in Finder/
  Explorer), and Clear Logs (truncates current log, preserves archives).

v0.7.4.20 - April 16, 2026
----------------------------
- NEW: Screen Layer Presets. Save the currently selected layer's settings as
  a reusable preset, then pick from saved presets when adding new screens.
  Presets are stored server-side in <app>/presets/*.json and shared across
  clients. Clicking "+ Add Screen" opens a picker showing each preset's
  dimensions and watts/panel; "💾 Save as Preset" captures ~90 layer
  properties (colors, data flow, power, labels, etc.), excludes identity,
  position, panels array, and runtime caches. Duplicate names prompt to
  overwrite; each preset row has a delete button.

v0.7.4.19 - April 7, 2026
----------------------------
- FIX: Backup ports now correctly counted in aggregate info labels (was always
  showing 0 because port assignments lacked backup type flags).
- FIX: Port counts now exclude hidden/blank panels and hidden layers.

v0.7.4.18 - April 7, 2026
----------------------------
- FIX: Power info label amp calculations now use correct formulas (I = P/V for
  single-phase, I = P/(V×1.73) for 3-phase) instead of circuit capacity math.
  Also accounts for panel load factors (half panels).
- UI: Removed circuits-per-phase suffix from 3-phase info label.

v0.7.4.17 - April 7, 2026
----------------------------
- FIX: Power Map color-coded view now shows correct circuit labels matching
  the normal view. Previously, toggling color view on/off caused labels to
  change because custom circuit numbering was not preserved in the color-coded
  rendering path.

v0.7.4.16 - April 7, 2026
----------------------------
- FIX: Restore native save/export dialogs for all connections. Removed the
  local-only restriction that forced LAN/remote clients to browser downloads.
  Save As and multi-file export now use the native dialog as before.

v0.7.4.15 - April 7, 2026
----------------------------
- UI: Right sidebar (Screens panel) now spans full height from toolbar to bottom,
  eliminating wasted space above the layer list.
- UI: Layer order buttons compacted to single row with smaller "▲ Up" / "▼ Down"
  buttons, removing the bordered box and label.
- FIX: Layer name input now stretches to fill available space up to the visibility
  button instead of being fixed at 80px width.

v0.7.4.13 - April 6, 2026
----------------------------
- FEATURE: HTTPS/SSL support. Self-signed certificate auto-generated on first use.
  Toggle in both Mac menu bar and Windows/Linux system tray launchers. Enables the
  browser File System Access API (native Save As dialog) when accessing over LAN.
- FEATURE: HTTPS toggle added to Windows/Linux system tray launcher (pystray),
  matching the Mac menu bar launcher. Status display now shows protocol (http/https).

v0.7.4.12 - April 6, 2026
----------------------------
- FEATURE: Text/Label layer (#24). New layer type for adding text annotations to the canvas.
  Movable, resizable, with font controls (bold/italic/underline, alignment, size, colors).
  Per-tab text content, each tab (Pixel Map, Cabinet ID, Data, Power) has its own text.
  Per-tab visibility checkboxes. Dynamic info toggles: Raster Size, Project Name, Date,
  Primary Ports, Backup Ports, Circuits, Single Phase, Three Phase.
- FEATURE: Project Notes panel in right sidebar (#24). Collapsible, resizable textarea
  between Screens and Help. Notes saved with the project file. Starts collapsed.
- FEATURE: Seamless server restart when changing network interface or port in the
  launcher menu. No app restart required, server hot-restarts and clients auto-reconnect.
- FIX: Remote/LAN file save. When accessing the app from another computer over the
  network, Save/Export now downloads files to the client browser instead of opening
  a file dialog on the server machine. Local access still uses native file dialogs.

v0.7.4.11 - April 1, 2026
----------------------------
- CHANGE: Recent Files max limit increased from 10 to 20.

v0.7.4.10 - April 1, 2026
----------------------------
- FIX: Recent Files label no longer disappears when files are present.
  Label text now in a span element preserved across shortcut label rewrites.
- Removed Clear Recent Files option. Removed unused handler.

v0.7.4.9 - April 1, 2026
----------------------------
- FIX: Recent Files submenu no longer destroyed by updateShortcutLabels().
  Submenu parent items are skipped during label rewrite. Click on submenu parent
  no longer closes the menu.

v0.7.4.8 - April 1, 2026
----------------------------
- FEATURE: Recent Files submenu under File menu. Tracks last 10 opened/saved projects
  in localStorage. Click to reload any recent project instantly.
- FEATURE: Transparent Background export option (#25). Checkbox in export modal removes
  the black background from PNG and PSD exports, rendering with alpha transparency.

v0.7.4.7 - April 1, 2026
----------------------------
- REVERT: Removed #20 focus preservation fixes, issue not reproducible on macOS.
  Will revisit when Windows reproduction steps are available.

v0.7.4.6 - April 1, 2026
----------------------------
- FIX: Port label inputs now preserve focus and in-progress edits during UI rebuilds.
  Added IDs to dynamically created port label inputs so focus restoration works. (#20)

v0.7.4.5 - April 1, 2026
----------------------------
- CHANGE: Port cycling shortcut changed from Shift+B/N to Tab/Shift+Tab.
  Only activates when custom mode is enabled and canvas has focus (not in sidebar fields).
  Updated Edit menu, context menu, and keyboard shortcuts dialog.

v0.7.4.4 - April 1, 2026
----------------------------
- FIX: Sidebar field focus no longer stolen when canvas redraws after property changes.
  Focus and cursor position are preserved across UI rebuilds. (Fixes #20)
- FIX: Horizontal trackpad scroll now pans the canvas instead of zooming out.
  Only vertical scroll triggers zoom. Tiny deltaY values ignored to prevent
  accidental zoom during horizontal swipes. (Fixes #6)

v0.7.4.3 - April 1, 2026
----------------------------
- FIX: Per-tab Screen Name checkboxes are now independent, unchecking on one tab no longer
  affects other tabs. updateLayerFromInputs() no longer overwrites per-tab label properties.
- FIX: Second resetApplicationState() now clears lastSelectedLayerId and selectionAnchorLayerId
  to match the primary reset method, preventing stale selection state after file load.
- FIX: Sidebar property changes (columns, rows, colors, checkboxes, font sizes, etc.) now
  create undo history entries. Uses debounced saveState (500ms) to avoid spam from color
  pickers and spinners. Cabinet ID style/position and screen name toggles save immediately.
- FIX: Move Layers undo now correctly reverts to pre-move position. saveState fires at
  drag start (mousedown) instead of drag end (mouseup).
- FIX: Custom edit mode toggle (data flow and power) now applies to all selected layers
  when multi-selecting, not just the current layer.

v0.7.4.2 - March 24, 2026
----------------------------
- FIX: Keyboard Shortcuts modal now shows Option/Alt for cross-platform clarity
- FIX: Alt+drag preventDefault to stop browser native drag behavior

v0.7.4.1 - March 24, 2026
----------------------------
- NEW: Alt+Click+Drag to bulk hide/show panels, paint mode based on first panel state
- NEW: Bulk panel visibility API endpoint for efficient server sync
- Added to Keyboard Shortcuts modal

v0.7.4 - March 24, 2026
----------------------------
- Version placeholder (superseded by v0.7.4.2)

v0.7.3 - March 22, 2026
----------------------------
- NEW: Resolume Polygon export for non-rectangular layers with hidden panels
- FIX: Layers with hidden panels export as Polygon contour instead of rectangular Slice
- FIX: Label bleed-through between overlapping layers

v0.6.5.18 - March 21, 2026
----------------------------
- FIX: Novastar Legacy port capacity now calculates using bounding rectangle instead of sum of visible panel pixels
- FIX: Non-rectangular layouts (stair-step, L-shape) now correctly account for the full rectangle the processor reserves

v0.6.5.17 - March 21, 2026
----------------------------
- NEW: Resolume Arena Advanced Output XML export, directly generates slice configuration from screen layers
- Each screen layer maps to a Resolume Slice with matching name, position, and pixel dimensions
- Export available in Export modal under "Resolume XML (Advanced Output)" format
- View checkboxes hidden when Resolume XML selected (geometry only, no rendered images)
- FIX: Resolume XML export now triggers browser Save As / download on all browsers (Safari, Chrome, Firefox)
- 7 new tests for Resolume XML export

v0.6.5.14 - March 21, 2026
----------------------------
- FIX: Layer name editing, double-click to edit name, single-click/drag for reorder

v0.6.5.13 - March 18, 2026
----------------------------
- CHANGED: Reverted to separate launcher_mac.py (rumps) and launcher_pc.py (pystray)
- CHANGED: Removed Tkinter launcher window, caused NSApplication crashes on macOS
- CHANGED: Removed Start Minimized option, tray/menu bar apps are always minimized
- CHANGED: Default network interface is localhost (127.0.0.1), opt-in for network access
- NEW: rumps menu bar with settings: network interface, port, run at login
- NEW: pystray system tray with same settings on Windows/Linux
- NEW: Network interface submenu with checkmarks for current selection
- NEW: Port change dialog (rumps window on macOS)
- NEW: Run at Login toggle (macOS LaunchAgent / Windows registry)
- NEW: Comprehensive CI build integrity checks (spec refs, version match, imports, static assets)
- NEW: 21 launcher settings tests (persistence, network detection, Run at Login)
- FIX: Slow macOS CI, cached get_network_interfaces() across test class
- FIX: PyInstaller spec references restored for separate launchers
- CHANGED: Settings persisted to JSON in platform config directory

v0.6.5.5 - March 18, 2026
----------------------------
- Change: Reorder numbering style options: A1,A2,A3 first, then 1,1 1,2 1,3, then A1,B1,C1
- Change: Default numbering style is now row-column (A1, A2, A3)

v0.6.5.4 - March 18, 2026
----------------------------
- Add: Keyboard shortcut Ctrl/Cmd+0 for Fit to View (Photoshop standard)
- Add: Keyboard shortcut Ctrl/Cmd+1 for Actual Size 1:1 (Photoshop standard)
- Add: Keyboard shortcut Ctrl/Cmd+Shift+' for Toggle Snap (Photoshop standard)
- Add: Toggle Snap option in View menu
- Update: Tooltips and Help > Keyboard Shortcuts modal with new shortcuts

v0.6.5.3 - March 17, 2026
----------------------------
- Fix: Canvas not re-rendering after server response on pixel-map and cabinet-id tabs
- Render was only called for power/data-flow modes after server update, causing visual changes to not appear

v0.6.5.2 - March 17, 2026
----------------------------
- Fix: Add Screen naming handles "Screen 1", "Screen_1" variants and uses layer count as minimum
- Fix: Drag-and-drop layer reordering works reliably on layer name text using pointer-events (Issue #5)

v0.6.5.1 - March 17, 2026
----------------------------
- Fix: Add Screen now auto-increments name based on highest existing screen number
- Fix: Drag-and-drop layer reordering now works when clicking on the layer name text (Issue #5)

v0.6.5 - March 17, 2026
----------------------------
 CHANGES:
 - Help tooltip panel is now permanently visible (removed close button).
 - Removed "Show Help Tooltip" menu item from Help menu (no longer needed).
 - Panel weight and preferences now support decimal values (e.g. 18.8 kg).

v0.6.4.9 - March 17, 2026
----------------------------
 FEATURES:
 - Preferences are now stored server-side and shared across all connected
   clients (Mac/Windows). No more per-browser localStorage divergence.
 - Color picker modal is now draggable by its header. Canvas remains
   pannable/zoomable while the color picker is open.
 - Increased help tooltip panel height for better readability.
 - Added zoom level tooltip to help panel.
 BUG FIXES:
 - Fixed "Fit" button tooltip saying "canvas" instead of "raster".

v0.6.4.8 - March 17, 2026
----------------------------
 BUG FIXES:
 - Fixed color picker on Windows targeting wrong color when opening for the
   second color swatch. The picker callback was captured at creation time and
   never updated; now uses dynamic callback reference so each swatch click
   correctly targets color1 or color2.

v0.6.4.7 - March 17, 2026
----------------------------
 BUG FIXES:
 - Fixed color picker crash on Windows (hexToRgbLocal not defined).
 - Fixed clicking on the selection marker ring picking white/black instead of
   the underlying wheel color, wheel is now redrawn clean before reading.
 - Fixed color picker auto-applying color when opening "Show Colors" modal
   on Windows, no longer changes the layer color until user picks one.
 - Fixed vertical brightness slider not working on Windows/Chrome/Edge by
   using cross-browser vertical slider CSS.
 - Fixed help tooltip panel appearing in wrong position, now correctly
   sits at the bottom of the right sidebar below the Screens panel.

 UI IMPROVEMENTS:
 - Added selection marker ring on the color wheel showing the currently
   picked position. Brightness slider now updates the color at the marker.
 - Added Resolume-style help tooltip panel in the bottom-right corner below
   the Screens panel. Hover any control to see contextual help. Closeable
   with X button, re-openable via Help > Show Help Tooltip.
 - Added comprehensive tooltips to all sidebar panels, toolbar buttons,
   view tabs, canvas controls, and input fields.
 - Replaced gear icon with a standard Preferences button next to Export.
 - Moved Offset Labels into the Labels panel, removing the separate Offsets
   section for a cleaner sidebar.
 - Color picker is now non-blocking, canvas can be panned and zoomed while
   the color picker is open. The color picker is also draggable by its header.

v0.6.4.3 - March 17, 2026
----------------------------
 UI IMPROVEMENTS:
 - Moved keyboard shortcuts and mouse controls out of the About dialog into
   their own dedicated modal accessible via Help > Keyboard Shortcuts.
 - About dialog now shows only version, author credit, and GitHub link.
 - Added "Mouse & Trackpad" section documenting pan, zoom, move, and toggle
   controls alongside keyboard shortcuts.

v0.6.4.2 - March 17, 2026
----------------------------
 BUG FIXES:
 - Fixed per-tab Screen Name checkbox cross-contamination, unchecking Screen Name
   on one tab (e.g. Pixel Map) no longer affects other tabs (Cabinet ID, Data Flow,
   Power). Each tab's Screen Name toggle now operates independently.
 - Root cause: loadLayerToInputs() was overwriting the pixel-map checkbox with the
   current tab's value, so updateLayerFromInputs() would write that value back to
   showLabelName, corrupting the pixel-map setting.

v0.6.4.1 - March 17, 2026
----------------------------
 CHANGES:
 - Version bump for continued development on dev/draft branch.

v0.6.4.0 - March 16, 2026
----------------------------
 BUG FIXES:
 - Fixed PNG export including selection overlays, layer selection borders, drag
   highlights, multi-select rectangles, custom path selection overlays, and active
   port/circuit badges are now suppressed during export via the existing exportMode flag.

v0.6.3.3 - March 13, 2026
----------------------------
 BUG FIXES:
 - Fixed raster size preferences not applying on startup, socket project_data event
   was overwriting preference-applied raster with server default (1920x1080).
 - Socket reconnect now preserves current raster size instead of resetting to server default.
 - Preference enforcement now syncs raster size to server so subsequent socket events
   return the correct values.

v0.6.3.2 - March 12, 2026
----------------------------
 BUG FIXES:
 - Fixed custom path undo removing 2 panels per undo instead of 1 (saveState ordering).
 - Fixed duplicate layer not applying halfFirstRow/halfLastRow until layer is moved.
 - Fixed duplicate/paste layer not copying data tab labels (port label templates, overrides,
   custom port paths, data flow port info, random colors).
 - Fixed startup preferences overwriting loaded projects on WebSocket reconnect after sleep.
 - Added server-side is_pristine flag to distinguish fresh default projects from loaded files
   named "Untitled Project", preventing preference enforcement on page refresh.
 - Added client-side _initialLoadComplete flag to block preference application on reconnect.

v0.6.3 - March 11, 2026
----------------------------
 NEW FEATURES:
 - Added automatic update checker, app checks GitHub Releases on startup for newer versions.
 - Added "Help" menu with "Check for Updates" button for manual update checks.
 - Added dismissable update notification banner with direct download link.
 - Update checker uses secure HTTPS with TLS 1.2+ and certificate pinning (certifi).
 - Added /api/update/check and /api/version API endpoints.

 CI/CD & TESTING:
 - Added CI workflow (ci.yml): flake8 lint, ESLint, pytest across Python 3.10/3.11/3.12.
 - Added Release workflow (release.yml): builds macOS .app and Windows .exe via PyInstaller
   with optional code signing/notarization, creates draft GitHub Release with SHA-256 checksums.
 - Added comprehensive test suite (573 lines across 5 test files):
   test_helpers.py, test_layers.py, test_panels.py, test_export.py, test_project.py, conftest.py.
 - Added requirements-dev.txt for development dependencies.

 BUG FIXES:
 - Fixed float-to-int conversion bug in render_layer_to_image, fractional pixel
   coordinates now correctly convert to integers before drawing.
 - Removed unused global declarations in save_project and delete_layer.
 - Fixed undo/redo for inline layer rename (double-click rename).
 - Fixed undo/redo for toggle panel blank.
 - Fixed undo/redo for toggle panel hidden (Alt+click).
 - Fixed undo/redo for layer visibility toggle.
 - Fixed undo/redo for layer lock toggle.
 - Fixed undo/redo not working for raster size changes.
 - Fixed multi-select offset fields overwriting each other on drag release and manual input.
 - After drag-moving multiple layers, offset inputs now properly show mixed-value indicator.

 ENHANCEMENTS:
 - Comprehensive undo/redo coverage for all layer property changes: colors, sizes,
   flow patterns, power settings, labels, borders, and all sidebar properties.
 - Changed updateLayers default to always save history.

 DOCUMENTATION & LEGAL:
 - Added professional README header with CI status badges.
 - Added proprietary LICENSE file for MeshTech Systems LLC.
 - Added multi-project tabs feature to TODO backlog.

v0.6.2.1 - March 8, 2026
----------------------------
 - Fixed multi-select offset fields overwriting each other on drag release and manual input.
 - After drag-moving multiple layers, offset inputs now properly show mixed-value indicator.
 - Added multi-project tabs feature to TODO backlog.

v0.6.2.0 - March 8, 2026
----------------------------
 - Added Mac .app bundle with menu bar icon (💡) for launch/quit control.
 - Added Windows system tray launcher with lightbulb icon.
 - Restructured project into src/ folder with Makefile build system (make mac / make windows / make clean).
 - Fixed logging path for bundled .app so logs appear next to the app, not buried inside the bundle.
 - Fixed Data tab port info not showing on file load (added fallback recalculation in canvas renderer).
 - Fixed file load not syncing all layer properties to server (added updateLayers for all layers on load).
 - Fixed labels overflowing layer bounds on all tabs (labels now clip to screen edges).
 - Fixed labels showing through higher layers (occlusion check on all tabs).
 - Fixed Data tab screen names disappearing on narrow columns (reset offset when outside layer bounds).
 - Fixed multi-select delete only removing one layer (now deletes all selected layers).
 - Fixed save filename using underscores instead of spaces.
 - Enhanced logging: removed HTTP noise, added tab/selection state to save_state, added copy/paste/visibility/reorder/panel toggle logging.
 - Updated README with correct build instructions (make mac / make windows).
 - Pushed to GitHub: github.com/kman1898/LED-Raster-Designer

v0.6.1.35 - February 28, 2026
----------------------------
 - Fixed undo coverage for layer rename actions (rename now writes history state before change).
 - Fixed undo coverage for Shift+drag screen-name moves in Cabinet/Data/Power (screen-name move now writes history state).
 - Prevented undo from jumping back to unrelated older actions in rename + shift-drag workflows.

v0.6.1.34 - February 27, 2026
----------------------------
 - Fixed Data start labels to render centered on first/last panel again (no left-edge offset behavior).
 - Fixed intermittent missing Power summary info on stacked/multi-screen layouts by always rendering Data/Power summary labels for each layer.
 - Kept corrected Power multi math (`ceil(circuits / 6)`) from v0.6.1.33.

v0.6.1.33 - February 27, 2026
----------------------------
 - Fixed Power info summary math to show correct multi count (`ceil(circuits / 6)`), so 10 circuits now shows `2 Multi`.
 - Hardened layer input loading to safely normalize `color1/color2` values and prevent `c1.r` runtime errors from interrupting UI updates.
 - Fixed Data start-label clipping at raster edges by clamping P/R label circles inside the raster bounds.
 - Improved Power info robustness by recalculating per-layer circuit/amp values at render time when cached values are missing.

v0.6.1.32 - February 24, 2026
----------------------------
 - Right-click on layer list now opens the context menu.
 - Global lock control is now a toggle icon; locked layers show a lock badge.
 - Removed per-layer duplicate/delete buttons; Delete is available in context menu.

v0.6.1.31 - February 24, 2026
----------------------------
 - Lock controls moved to a global “Lock Selected / Unlock Selected” toolbar.
 - Removed per-layer duplicate/delete buttons; moved Delete to right-click menu.
 - Selection rectangle now clears on any mouseup (reduces stuck multi-select boxes).

v0.6.1.30 - February 24, 2026
----------------------------
 - Added layer lock toggle (prevents position changes for locked layers).
 - Locked layers are excluded from drag moves and offset input changes.
 - Extra safety clears layer selection rectangle if state desyncs mid-render.

v0.6.1.29 - February 24, 2026
----------------------------
 - Layer selection/drag box now force-clears if selection state desyncs (prevents stuck rectangle).
 - Added selection logging (start/end/box) to improve diagnostics.

v0.6.1.28 - February 24, 2026
----------------------------
 - Pixel Map hides screen-only panels when an image layer is selected (image controls only).
 - Canvas click now prioritizes topmost layer selection (image layers no longer select underlying screens).
 - Added client logs for raster size changes (helps diagnose unexpected 1920x1080 resets).

v0.6.1.27 - February 24, 2026
----------------------------
 - Restored Pixel Map sidebar visibility for image layers.
 - Prevented layer selection box from sticking when mouseup occurs off-canvas.

v0.6.1.26 - February 24, 2026
----------------------------
 - Image layers now show correct sidebar controls (no blank panel).
 - Image alpha PNGs render with transparency intact.

v0.6.1.25 - February 24, 2026
----------------------------
 - Image layers hide non-relevant controls in the left sidebar.

v0.6.1.24 - February 24, 2026
----------------------------
 - Image layers render immediately on add and scale live while dragging the slider.
 - Fixed right sidebar clipping in the Screens list.

v0.6.1.23 - February 24, 2026
----------------------------
 - Added Image/Logo layer type with scale control and canvas rendering.
 - Image layers support add/replace, reorder, save/load, and undo/redo.

v0.6.1.22 - February 22, 2026
----------------------------
 - Prevented horizontal scrolling in the screen layer list (always fully visible).

v0.6.1.21 - February 17, 2026
----------------------------
 - Documentation corrections (README accuracy pass).
 - Reordered Power controls again for workflow clarity:
   - `Custom Power Path` now sits below `Maximize Power Use` and `Organized`
   - `Custom Power Path` remains above the `Circuit Labels` section

v0.6.1.20 - February 17, 2026
----------------------------
 - Reordered Power sidebar sections:
   - Moved Voltage/Amperage/Watts and computed power stats directly under `Show Circuit Info`.
   - Moved `Custom Power Path` directly under `Circuit Color View`.
 - Kept all existing control IDs/behavior intact so current logic and undo/redo flows continue to work.

v0.6.1.19 - February 17, 2026
----------------------------
 - Fixed Power circuit color class behavior so circuit color edits apply across the whole screen by circuit slot (1-6), not just the first multi.
 - Updated Power circuit color editor labels from `Circuit A-F` to `Circuit 1-6`.
 - Updated Circuit 4 default color to `#2CF82B`.
 - Added migration so older default Circuit 4 color `#79FC4C` is automatically normalized to `#2CF82B`.

v0.6.1.18 - February 17, 2026
----------------------------
 - Investigated intermittent Power label-size edit UI shift from logs:
   - No raster/zoom/project state mutation observed at the event time; only normal `power-label-size` update actions.
 - Added stable scrollbar gutters to prevent layout jitter when side-panel/list scrollbars appear/disappear:
   - left/right sidebars
   - layers list
   - data/power label editor lists

v0.6.1.17 - February 17, 2026
----------------------------
 - Added Data label color controls:
   - `Primary Text` color
   - `Redundancy Text` color
 - Added Power label color controls:
   - `Label Background` color
   - `Label Text` color
 - Data and Power start-label rendering now uses these configurable label background/text colors.
 - Added persistence for new label color fields across save/load, duplicate, paste, and layer updates.

v0.6.1.16 - February 17, 2026
----------------------------
 - Reverted Power circuit label color customization changes from v0.6.1.15.
 - Restored Power start label rendering to fixed default style/color.
 - Moved top toolbar actions (`New`, `Open`, `Save`, `Export`) to the right side of the project name area.

v0.6.1.14 - February 17, 2026
----------------------------
 - Fixed tab border color coupling: Pixel Map, Cabinet ID, Data, and Power now use independent border colors.
 - Changing border color in one tab no longer changes border colors in other tabs.
 - Added client-side persistence for per-tab border colors across layer updates, save/load, duplicate, and paste.

v0.6.1.13 - February 17, 2026
----------------------------
 - Fixed Power `Circuit Color View` start labels to render horizontal (no rotation).
 - Fixed color mode labeling to show only one start label per circuit (no per-panel ID text).
 - Color mode start labels now use standard Power circuit label text (`S1-#` + overrides) for consistency with serpentine mode.

v0.6.1.12 - February 15, 2026
----------------------------
 - Enhanced Power `Circuit Color View` to match documentation workflow:
   - Added circuit start labels in color mode (`A1`, `B1`, etc.) with gray box + directional pointer.
   - Kept per-panel identifiers (`A1...`, `B1...`) centered in each panel.
 - Added customizable circuit color mapping (`A`-`F`) with multi-select apply UI in Power sidebar.
 - Removed Power export legend extension so exports now capture exactly what is on canvas in color mode.
 - Added persistent `powerCircuitColors` layer property across client/server save/load/update paths.

v0.6.1.11 - February 15, 2026
----------------------------
 - Added Power tab `Circuit Color View` toggle for color-coded panel fills by circuit assignment.
 - Added panel-level color-mode power labels (`A1/A2/B1...`) with per-circuit numbering restart and contrast-aware text.
 - Added Power export legend layout (left-side circuit swatch legend) when color-coded view is enabled.
 - Export pipeline now carries per-view image dimensions for PDF/PSD generation to support legend-extended power exports.

v0.6.1.10 - February 15, 2026
----------------------------
 - Fixed refresh fallback to 1920x1080 for untitled startup project by consistently reapplying startup defaults on load.
 - Preserved non-untitled project protection so external loaded files are not overridden by startup defaults.
 - Improved file-open input handling (DOM-attached file input + explicit open/cancel logging) for more reliable JSON load behavior.

v0.6.1.9 - February 15, 2026
---------------------------
 - File-load fallback now uses Data flow pattern when `powerFlowPattern` is missing/blank in project JSON.
 - Cleared transient computed fields (`_powerError`, `_powerCircuits`, `_capacityError`, etc.) when loading files so stale values cannot trigger incorrect first render.
 - Reduced first-load inconsistency where power/data state corrected only after changing settings.

v0.6.1.8 - February 15, 2026
---------------------------
 - Fixed startup raster default regression by applying preference raster size during startup default project initialization.
 - Tightened startup preference enforcement in client property load path to true factory-default untitled project only.
 - Prevented loaded untitled single-layer files from being overwritten by startup preference enforcement.

v0.6.1.7 - February 15, 2026
---------------------------
 - Fixed loaded-project refresh regression where startup defaults were being reapplied to some non-default single-layer files.
 - Tightened startup-default detection so preference auto-apply only runs for a true factory default layer.
 - Restricted local id-based client property restore to startup default project only, preventing cross-project load contamination.

v0.6.1.6 - February 15, 2026
---------------------------
 - Fixed organized Data/Power path sequencing so each new port/circuit starts from the expected side for the selected flow pattern.
 - Fixed project-refresh raster regression by applying server project raster size on load and websocket project sync.
 - Added explicit raster sync to toolbar/canvas when restoring project data.

v0.6.1.5 - February 12, 2026
---------------------------
 - Documentation consistency pass completed across README/TODO/VERSION and developer handoff docs.
 - Added explicit source-of-truth guidance in historical handoff docs to reduce stale-reference confusion.
 - Standardized user-facing wording to `Data` where applicable.

v0.6.1.4 - February 12, 2026
---------------------------
 - Moved Raster width/height controls to the canvas control bar (top-left of canvas area).
 - Raster controls now appear directly left of zoom controls for faster workflow access.
 - Added roadmap item for image/logo layer type with dedicated settings and Shift+drag movement behavior.

v0.6.1.3 - February 12, 2026
---------------------------
 - Preferences Save/Reset now only apply defaults to the startup default untitled project layer.
 - Open non-default projects no longer have raster/defaults overwritten when updating Preferences.
 - Added preference save/reset diagnostics (`preferences_saved`, `preferences_reset`) including whether defaults were applied to current project.
 - Completed focused formula verification for `lb` unit + half-row/half-column behavior in Data/Power/Weight calculations.

v0.6.1.2 - February 12, 2026
---------------------------
 - Save/Export native dialog cancel now exits cleanly without fallback auto-download to Downloads.
 - Pixel Map Info label size control changed to numeric input (beside Info toggle).
 - Increased label-size input caps to 200 in Pixel Map/Cabinet/Data/Power controls.
 - Release zip policy: logs excluded so distributed builds start with empty logs.

v0.6.1.1 - February 12, 2026
---------------------------
 - Renamed the Data Flow tab label to Data for simpler UI language.
 - Added panel weight unit support (`kg`/`lb`) with Preferences defaults.
 - Added independent Pixel Map Info label size slider (per-layer).
 - Added Preferences defaults for Data label size and Power label size.
 - Preserved `panel_weight`, `weight_unit`, and `infoLabelSize` across single/multi layer update round-trips.
 - Updated Pixel Map weight labels/info to convert correctly when panel weight unit is `lb`.
 - Log pruning now removes legacy log files and enforces configured archive retention.

v0.6.1.0 - February 12, 2026
---------------------------
 - Logging policy updated to a persistent active file `logs/led_raster_designer.log` (not per-session files).
 - Rotation still uses 20 MB x 3 files total; when rotated, archives are timestamped (`led_raster_designer_YYYYMMDD_HHMMSS.log`) and oldest archives are pruned.
 - Added half-cabinet geometry controls per screen layer:
   - Half First/Last Column
   - Half First/Last Row
 - Half-cabinet geometry now updates panel generation and layer bounds in server/client rendering paths.
 - Data port capacity/assignment now uses per-panel pixel area (supports half rows/columns).
 - Power capacity now uses 65% load for half cabinets (via area-based factor), including total amps and circuits required.
 - Panel weight labels now use the same half-cabinet load factor as power (65% for half cabinet).

v0.6.0.0 - February 12, 2026
---------------------------
 - V6 milestone release.
 - Added new Pixel Map label toggle: `Panel Weight`.
 - Panel weight label uses active panels only (`!hidden && !blank`) so hidden/deleted panels are excluded from total weight calculation.
 - Logs now write to `logs/` as timestamped session files and rotate at 20 MB x 3 files.

v0.5.6.42 - February 12, 2026
---------------------------
 - Log output now writes to `/logs` with timestamped session files (`led_raster_designer_YYYYMMDD_HHMMSS.log`) so newest sessions sort naturally.
 - Kept 20 MB x 3-file rotation active per session log file.
 - Fixed data-port bulk rename reversion by preserving port-label templates/overrides/custom paths during websocket layer updates.
 - Improved multi-layer editing persistence for screen-name toggles and Data/Power info toggles by updating all selected layers to server state.

v0.5.6.41 - February 12, 2026
---------------------------
 - Removed blocking Save/Export warning/error alert popups and replaced them with diagnostic log events + status updates.
 - Confirmed log rotation policy remains 20 MB per file with 3 total files (`led_raster_designer.log`, `.1`, `.2`).
 - Retained native Save/Export write diagnostics for path/size verification.

v0.5.6.40 - February 12, 2026
---------------------------
 - Preferences now use visual 8-pattern snake selectors for Data Flow and Power Flow defaults.
 - Added persistent power flow pattern preference (`powerFlowPattern`) and applied it to new/default layers.
 - Save/Export native fallback now writes multi-export files one-by-one with per-file diagnostics to avoid silent failures on large payloads.
 - Added more native dialog server logs (start/cancel/validation/write verification) for Save/Export troubleshooting.

v0.5.6.39 - February 12, 2026
---------------------------
 - Fixed layer-update property preservation so Data defaults (processing, bit depth, frame rate) no longer revert after layer interactions.
 - Preserved full Data/Power client-side fields during single-layer updates to prevent unintended fallback values.
 - Kept subprocess native dialog export/save flow in place for stable non-download fallback behavior.

v0.5.6.38 - February 12, 2026
---------------------------
 - Startup load now force-applies Preferences defaults to untitled default project layer before first interaction.
 - Replaced native dialog backend from `tkinter` to subprocess-based OS dialogs (macOS `osascript`, Windows PowerShell, Linux `zenity` fallback) to avoid Flask-thread GUI crashes.
 - Save/Export native fallback now uses stable OS dialogs without terminating Python process.

v0.5.6.37 - February 12, 2026
---------------------------
 - Layer select now logs before/after processor/bit-depth/frame-rate values and enforces Preferences for startup untitled single-layer project.
 - Added native OS save-file fallback via Python (`tkinter`) when browser file picker APIs are unavailable.
 - Added native OS folder picker + multi-file write fallback for exports when browser directory/file picker APIs are unavailable.
 - Save/Export can now target user-selected paths even in browsers that force normal downloads.

v0.5.6.36 - February 12, 2026
---------------------------
 - Startup untitled/default project now enforces saved Preferences for processor, bit depth, frame rate, and power defaults every launch.
 - Added capability logging for Save/Export file picker APIs to simplify browser-specific diagnostics.
 - Added one-time user warning when browser lacks in-app file picker APIs (downloads fallback behavior).

v0.5.6.35 - February 12, 2026
---------------------------
 - Preferences and Export modals now close only on true backdrop click (press+release on backdrop), fixing drag-select text close bugs.
 - Save Project now awaits file picker flow so user-selected save location is honored when picker APIs are available.
 - Preferences save now reapplies defaults to the active default layer and persists client-side properties.
 - Startup default-layer preference application no longer depends on hard-coded geometry checks.
 - Multi-export non-FSA fallback now staggers file downloads to improve reliability when browsers block simultaneous downloads.

v0.5.6.34 - February 11, 2026
---------------------------
 - Multi-export now falls back to sequential save dialogs when directory picker is unavailable (prevents single-file-only behavior).
 - New layer defaults now use Preferences for processing type, bit depth, frame rate, voltage, amperage, and watts.
 - Data Flow processor/bit-depth/frame-rate input fallbacks now use Preferences defaults.
 - Power circuit labels are centered on the start panel to match Data Flow label centering.

v0.5.6.33 - February 10, 2026
---------------------------
 - Windows color modal now includes RGB/HSB/Gray/CMYK/Spectrum modes.
 - Color modal adds mode bar and slider UI for macOS-style modes.

v0.5.6.32 - February 10, 2026
---------------------------
 - Windows color popover now clamps inside viewport (no cut-off picker).
 - Custom color modal no longer closes on drag-release outside.

v0.5.6.31 - February 10, 2026
---------------------------
 - Preferences modal no longer closes when mouse drag ends outside the dialog.
 - Preferences now include power dropdowns (Voltage/Amperage) and panel size in mm.

v0.5.6.30 - February 10, 2026
---------------------------
 - Preferences now include defaults for processing type, bit depth, frame rate, voltage, amperage, and watts.

v0.5.6.29 - February 10, 2026
---------------------------
 - Power over-capacity now suppresses circuit lines and arrows (error overlay only).
 - Power label circles now auto-size to fit text.

v0.5.6.28 - February 10, 2026
---------------------------
 - Project save now uses file picker for JSON and persists raster size in the JSON.
 - Power circuit labels now use the same circular style as data labels.

v0.5.6.27 - February 10, 2026
---------------------------
 - Raster size now stored on project updates and when applying preferences.
 - JSON load uses PUT /api/project to restore full project data.
 - Drag reorder indicator now shows an insertion line between layers.

v0.5.6.26 - February 10, 2026
---------------------------
 - Preferences raster size now re-applies after client-props version resets.
 - Layer drag reorder now works from header/info areas (Safari-friendly).

v0.5.6.25 - February 10, 2026
---------------------------
 - Preferences-based raster size now applies on app open after server restart.
 - JSON load now renders immediately and logs start/success/failure; server sync failures no longer block load.

v0.5.6.24 - February 10, 2026
---------------------------
 - Layer reorder arrows moved to a dedicated control (no layer-list horizontal scroll).
 - Canvas render now guards against missing project layers to prevent crash on load.
 - JSON load now validates server response before applying to the UI.

v0.5.6.23 - February 10, 2026
---------------------------
 - Menu shortcuts now show platform-appropriate keys (Cmd on macOS, Ctrl on Windows).
 - Power info line no longer zeros out when changing flow pattern.
 - Data/Power start labels are offset from lines and render above arrows.
 - Preferences now apply checkerboard colors correctly on new projects.
 - Data Flow pattern changes re-run capacity/label calculations after save.
 - Data/Power labels now recalc on every UI refresh to avoid zeroed info.
 - Preferences apply + input sync prevents circle/X toggle reset.
 - Preferences now include cabinet font size and pixel-map label font size.
 - New layers include power defaults so power diagram renders without manual watt change.
 - Preferences reset button added; defaults apply on app open to the default layer.
 - Removed internal archive folder; moved old zips to main Archive.
 - Power info label now uses “Multi” instead of “SOCA”.
 - Data/Power info labels follow screen name size.
 - Loading JSON now syncs server project and applies missing power defaults.
 - Layer list now supports drag reorder and up/down arrows.

v0.5.6.17 - February 10, 2026
---------------------------
 - Export: multi-view PNG/PSD now saves as separate files (no zip) with editable suffixes.
 - Export: filename suffixes are saved as defaults; Cabinet/Data/Power rename to Map naming.
 - Export: folder/file picker used when browser supports it (directory for multi, save dialog for single).
 - Added File/Edit/View menu bar and right-click context menu with shortcuts.
 - Preferences modal added for default raster, grid, colors, flow pattern, and line widths.

v0.5.6.16 - February 10, 2026
---------------------------
- Power info line now stays visible when voltage/amperage change (shows 0 values if needed).

v0.5.6.15 - February 10, 2026
---------------------------
- Data/Power info line now anchors under screen name and shows amps for power.

v0.5.6.7 - February 10, 2026
---------------------------
- macOS: force-hide all swatch buttons (native picker only).

v0.5.6.6 - February 10, 2026
---------------------------
- macOS: removed redundant swatch (native picker only).
- Windows/Linux: swatch retained for custom picker.

v0.5.6.2 - February 9, 2026
---------------------------
- Power tab: fixed circuit rendering after changing flow patterns.
- Power tab: custom mode moved near flow pattern, grid layout normalized.
- Power tab: Maximize and Organized now mutually exclusive.

v0.5.6.0 - February 9, 2026
---------------------------
- Power tab overhaul: circuit-based serpentine routing, voltage/amperage/watts inputs.
- Calculated power display with total amps for 1φ and 3φ.
- Power custom path mode and flow pattern support.

v0.5.5.42 - February 6, 2026
---------------------------
- Updated default screen colors to #404680 and #959CB8.

v0.5.5.41 - February 6, 2026
---------------------------
- Fixed checkerboard color pickers to use the custom/native picker pipeline.

v0.5.5.40 - February 6, 2026
---------------------------
- Custom picker: corrected macOS swatch order and layout.
- Added "no color" swatch styling in the popover (non-destructive).
- Custom picker eyedropper now uses icon button.
- macOS native color input styled to match swatch sizing.

v0.5.5.39 - February 6, 2026
---------------------------
- macOS: use native system color picker; Windows/Linux: custom macOS-style picker.
- Added EyeDropper support in custom picker where browser supports it.
- Swatch order updated to match macOS layout.

v0.5.5.38 - February 6, 2026
---------------------------
- Custom macOS-style color picker (swatches + modal) replacing native pickers.
- README: updated to current version and noted custom color picker.

v0.5.5.37 - February 6, 2026
---------------------------
- README: rewritten install section with Windows/Mac/Linux instructions.
- Canvas: clicking empty space no longer deselects the current layer.
- Port label bulk apply now restarts numbering from 1 for each selected group.

v0.5.5.36 - February 6, 2026
---------------------------
- README updated to document all current features and usage.

v0.5.5.35 - February 6, 2026
---------------------------
- Layer name editing: double-click now enters edit; single click stays selection.

v0.5.5.34 - February 6, 2026
---------------------------
- Layer list: name field no longer focuses on single-click; double-click to edit.
- Selected layers now show per-layer bounding boxes (Photoshop-style).

v0.5.5.33 - February 6, 2026
---------------------------
- Layer list: single-click selects without entering edit; double-click to rename.
- Canvas drag-select now shows a selection box and darkens intersected layers.
- Added visual distinction for primary selected layer in the list.

v0.5.5.32 - February 6, 2026
---------------------------
- Photoshop-style multi-select: Shift+click range + Cmd/Ctrl toggle in layer list.
- Canvas drag-select to multi-select layers; Cmd/Ctrl toggle on canvas click.
- Sidebar edits now apply to all selected layers, with mixed-value display.

v0.5.5.31 - February 6, 2026
---------------------------
- Data Flow: port label list now updates immediately when processor/bit depth/frame rate/mapping/pattern change.

v0.5.5.30 - February 6, 2026
---------------------------
- Custom Data Flow: when no custom paths exist, port count now falls back to capacity-based ports.

v0.5.5.29 - February 6, 2026
---------------------------
- Data Flow tab now performs a deferred port list refresh to fix initial list underpopulation.
- Port label editor debug now logs rendered row count.

v0.5.5.28 - February 6, 2026
---------------------------
- Added debug logging for port capacity + port label list to help diagnose startup port list issue.

v0.5.5.27 - February 6, 2026
---------------------------
- Port label list now recalculates ports on Data Flow entry to avoid showing only Port 1.

v0.5.5.26 - February 6, 2026
---------------------------
- Data Flow tab now refreshes port capacity + port label list on entry.

v0.5.5.25 - February 6, 2026
---------------------------
- Port Labels panel: added Select All and Deselect All for bulk renaming.

v0.5.5.24 - February 6, 2026
---------------------------
- README updated with current features, shortcuts, processors, and custom mode notes.

v0.5.5.23 - February 6, 2026
---------------------------
- Custom mode now clears any capacity error overlay when enabled.
- Custom path edits now persist to localStorage after add/clear/pattern actions.
- Custom selection highlight darkened for clearer visibility.
- Custom pattern debug logging now includes first/last panel and grid size.

v0.5.5.22 - February 5, 2026
---------------------------
DATA FLOW: Selection UX + shift-drag precedence

**Completed:**
- ✅ Custom selection no longer participates in undo/redo history
- ✅ Selection highlight now darkens panels for visibility
- ✅ Shift+drag screen-name move supersedes custom selection


v0.5.5.21 - February 5, 2026
---------------------------
DATA FLOW: Space+drag pan overrides selection

**Completed:**
- ✅ Space+click now pans instead of starting selection in custom mode


v0.5.5.20 - February 5, 2026
---------------------------
DATA FLOW: Custom selection undo support

**Completed:**
- ✅ Custom selection state is now part of undo/redo history
- ✅ Selection changes are captured on mouse-up


v0.5.5.19 - February 5, 2026
---------------------------
DATA FLOW: Selection locks path editing

**Completed:**
- ✅ Clicking while selection exists now clears selection instead of adding panels
- ✅ Custom path clicks are ignored when a selection is active


v0.5.5.18 - February 5, 2026
---------------------------
DATA FLOW: Active port badge in canvas corner

**Completed:**
- ✅ Active port badge now renders in top-left of canvas (screen space)


v0.5.5.17 - February 5, 2026
---------------------------
DATA FLOW: Single-panel port label order

**Completed:**
- ✅ In custom flow, single-panel ports now show Primary label on top


v0.5.5.16 - February 5, 2026
---------------------------
DATA FLOW: Custom mode shortcuts + port badge position

**Completed:**
- ✅ Shift+N for next port, Shift+B for previous
- ✅ Active port badge now anchored to top-left of canvas


v0.5.5.15 - February 5, 2026
---------------------------
FIX: Custom flow label rendering crash

**Completed:**
- ✅ Fixed circleRadius scope error in custom flow rendering


v0.5.5.14 - February 5, 2026
---------------------------
DATA FLOW: Custom mode fixes (labels, selection, persistence)

**Completed:**
- ✅ Custom data saved/restored via client-side props
- ✅ Drag selection uses panel intersection (not full containment)
- ✅ Added debug toggle for custom mode console logging


v0.5.5.13 - February 5, 2026
---------------------------
DATA FLOW: Custom mode stability + shortcut change

**Completed:**
- ✅ Added Cmd/Ctrl+N for next custom port
- ✅ Prevented selection from flooding undo history


v0.5.5.12 - February 5, 2026
---------------------------
DATA FLOW: Custom path UX fixes + undo/redo support

**Completed:**
- ✅ Click to add panels in custom mode now works; drag selects panels automatically
- ✅ Added editable Active Port with prev/next buttons and [ / ] shortcuts
- ✅ Added large on-screen active port badge in custom mode
- ✅ Custom path actions now save to undo/redo history


v0.5.5.11 - February 5, 2026
---------------------------
DATA FLOW: Custom Path mode (manual + pattern on selection)

**Completed:**
- ✅ Added Custom Path mode toggle with per-port paths
- ✅ Click panels or use arrow keys to build port paths (skips hidden)
- ✅ Select panels and apply built-in patterns to the selection


v0.5.5.10 - February 5, 2026
---------------------------
UI: Processing label and NovaStar legacy naming

**Completed:**
- ✅ Renamed "Processor" label to "Processing"
- ✅ Renamed first NovaStar option to "NovaStar (Legacy)"


v0.5.5.9 - February 5, 2026
---------------------------
DATA FLOW: Port label token simplified

**Completed:**
- ✅ Replaced {n} with # for port number templating
- ✅ Renamed "Bulk Override" UI to "Apply Style"


v0.5.5.8 - February 5, 2026
---------------------------
DATA FLOW: Editable port labels (template + per-port overrides)

**Completed:**
- ✅ Added Port Labels editor in Data Flow panel (template + per-port overrides)
- ✅ Port label text now supports {n} templating and overrides in render


v0.5.5.7 - February 5, 2026
---------------------------
DEFAULTS: NovaStar Armor set as default processor

**Completed:**
- ✅ Default processor set to NovaStar Armor (legacy 1G)


v0.5.5.6 - February 5, 2026
---------------------------
CAPACITY: Legacy NovaStar Armor capped at 120 Hz

**Completed:**
- ✅ Armor (MSD/MRV) table capped at 120 Hz
- ✅ Frame-rate dropdown now limits Armor to 120 Hz max


v0.5.5.5 - February 5, 2026
---------------------------
CAPACITY: Split NovaStar systems (Armor vs COEX 1G)

**Completed:**
- ✅ Added NovaStar Armor (MSD/MRV) processor key with rectangle constraint
- ✅ Renamed NovaStar COEX 1G key to `novastar-coex-1g` and migrated old `novastar-1g`
- ✅ Updated UI labels and defaults to match the three NovaStar systems


v0.5.5.4 - February 5, 2026
---------------------------
CAPACITY: Added NovaStar Armor (Other 1G) receiving cards

**Completed:**
- ✅ Added NovaStar Armor (Other 1G) processor option and lookup table
- ✅ Applied rectangle-constraint rules to Armor (same as NovaStar 1G)


v0.5.5.3 - February 5, 2026
---------------------------
EXPORT: Crisper pixel-to-pixel output (no UI change)

**Completed:**
- ✅ Export rendering now snaps key draw positions to whole pixels (export mode only)
- ✅ Data flow line widths snap to whole pixels in export mode for cleaner edges
- ✅ Label backgrounds and text positions snap during export to reduce soft edges


v0.3.9.6 - January 28, 2026
---------------------------
DATA FLOW TAB: Complete serpentine arrow rendering

**Completed:**
- ✅ Updated flow pattern button icons to match Pixel Perfect Pro style
  - 2x2 grid preview with green start dot and red end arrow
  - 8 patterns: all corner starts × horizontal/vertical-first
- ✅ Implemented serpentine path rendering on canvas
  - Lines connect panel centers following selected pattern
  - Directional arrows at midpoints between panels
- ✅ P1 (Primary) label with green circle at flow start
- ✅ R1 (Return) label with red circle at flow end
- ✅ Flow pattern selection UI with active state indication
- ✅ Configurable: Line width, arrow size, P1 color, R1 color, line color
- ✅ Random Colors checkbox and Color picker for data lines

**Pattern Reference:**
Row 1: TL-H, TL-V, TR-H, TR-V (top start patterns)
Row 2: BL-H, BL-V, BR-H, BR-V (bottom start patterns)
Where: H=horizontal-first serpentine, V=vertical-first serpentine


v0.3.9.5 - January 28, 2026
---------------------------
CHECKPOINT: Data Flow UI in progress - continue in new thread

**Status:**
- ✅ Cabinet ID tab COMPLETE and working (3 numbering styles, 2 positions)
- 🔄 Data Flow tab UI created but SVG patterns need correction
- ⏸️ Power tab not started

**What's Done:**
- Data Flow controls added (line width, arrow size, colors)
- 8 pattern buttons created (need visual correction)
- Backend storage for all Data Flow settings
- Settings preserved on duplicate/paste

**Next Thread Tasks:**
1. Fix the 8 SVG pattern buttons to match Pixel Perfect Pro reference
2. Wire up button click handlers in JavaScript
3. Implement rendering logic for:
   - Arrows between panels based on flow pattern
   - Green P1 label at primary start
   - Red R1 label at backup end
   - Serpentine path visualization
4. Move to Power tab implementation

**Token Usage:** 75% - Starting fresh thread for cleaner implementation

**Reference Images Provided:**
- Pixel Perfect Pro data flow diagrams showing 8 serpentine patterns
- Each pattern has: Green arrow (start), white curved path, Red arrow (end)


v0.3.9.4 - January 28, 2026
---------------------------
SIMPLIFICATION: Just S-shapes in 8 orientations

**User Feedback:**
"It's literally an S oriented in different ways. One side has a dot at the start, then a flow arrow, and an arrow at the end."

**Changes:**
- ✅ Simplified to basic S-shapes (serpentine patterns)
- ✅ Dot at start (white circle)
- ✅ S-shaped path
- ✅ Arrow at end

**8 Orientations:**
1. Top-left S going right-down
2. Top-center vertical S down
3. Top-right S going left-down  
4. Top-right reversed S down-up
5. Middle-left horizontal
6. Bottom-center vertical up
7. Middle-right horizontal left
8. Bottom-right S up

Much simpler and clearer!


v0.3.9.3 - January 28, 2026
---------------------------
FIX: Corrected flow pattern SVG diagrams to match reference

**What Was Wrong:**
My SVGs were way too complex with serpentine paths. The actual patterns are much simpler!

**Corrected 8 Patterns:**
Row 1:
1. Top-Left L-shape (down then right)
2. Top center vertical down  
3. Top-Right reverse L (down then left)
4. Top-Right U-shape down (goes down, across, back up)

Row 2:
5. Middle-Left horizontal right
6. Bottom center vertical up
7. Middle-Right horizontal left
8. Bottom-Right U-shape up (goes up, across, down)

Each pattern now correctly shows:
- White dot/circle at START
- Simple line path
- Arrow at END (or circle for endpoints)


v0.3.9.2 - January 28, 2026
---------------------------
UI: Visual flow pattern diagrams with SVG icons

**Changes:**
- ✅ Replaced text icons with proper SVG diagrams
- ✅ Each button shows: dot at start, lines with arrows, final arrow at end
- ✅ 8 patterns match Pixel Perfect Pro style

**Pattern Descriptions:**
Row 1:
1. Horizontal L→R Serpentine (Z-pattern left start)
2. Vertical T→B Serpentine (columns, left to right)
3. Horizontal R→L Serpentine (Z-pattern right start)
4. Vertical B→T Serpentine (columns bottom-up)

Row 2:
5. Horizontal L→R Serpentine Alt (starts middle)
6. Vertical T→B Serpentine Alt (columns, right to left)
7. Horizontal R→L Serpentine Alt (starts middle)
8. Vertical B→T Serpentine Alt (columns, starts right)

**Next Step:** Wire up button click handlers and implement rendering logic


v0.3.9.1 - January 28, 2026
---------------------------
UX IMPROVEMENT: Simplified Data Flow UI to match Pixel Perfect Pro

**Changes Based on Feedback:**
- ✅ Removed "Show Data Flow" checkbox (redundant on Data Flow tab)
- ✅ Removed "Start Position" radio buttons (handled by flow pattern)
- ✅ Removed "Show Backup Input" checkbox (redundancy always shown)
- ✅ Replaced dropdown with **visual button grid** (4x2 = 8 patterns)
- ✅ Added **Line Width** control
- ✅ Simplified color labels: "Primary Color" and "Backup Color"
- ✅ Pattern buttons show visual icons (arrows)

**8 Flow Patterns (Button Grid):**
Row 1 (Top-Left start): Horizontal Serpentine, Vertical Serpentine, Horizontal Straight, Vertical Straight
Row 2 (Top-Right start): Horizontal Serpentine, Vertical Serpentine, Horizontal Straight, Vertical Straight

**Controls:**
- Line Width (1-8px)
- Arrow Size (6-24px)
- Arrow Color
- Primary Color (green P1 label)
- Backup Color (red R1 label)

**Next Step:** Implement rendering logic for arrows and P1/R1 labels


v0.3.9 - January 28, 2026
-------------------------
FEATURE: Data Flow Tab UI (Part 1 - UI Only)

**New Data Flow Controls:**
- ✅ Show Data Flow checkbox
- ✅ Flow Pattern dropdown (8 patterns):
  - Horizontal L→R (Serpentine)
  - Horizontal R→L (Serpentine)
  - Vertical T→B (Serpentine)
  - Vertical B→T (Serpentine)
  - Horizontal L→R (Straight)
  - Horizontal R→L (Straight)
  - Vertical T→B (Straight)
  - Vertical B→T (Straight)
- ✅ Start Position radio buttons (Top-Left, Top-Right, Bottom-Left, Bottom-Right)
- ✅ Show Backup Input checkbox (for R1 label)
- ✅ Arrow Size control
- ✅ Arrow Color picker
- ✅ Primary Label Color (green P1)
- ✅ Backup Label Color (red R1)
- ✅ Backend layer model updated with all settings
- ✅ Settings preserved on duplicate/paste

**Next Step (v0.3.10):**
Implement rendering logic for arrows, P1/R1 labels, and flow visualization


v0.3.8.4 - January 28, 2026
---------------------------
BUG FIX: Cabinet ID numbers now clipped to raster bounds

**What Was Wrong:**
Cabinet ID numbers were rendering outside the raster box (the red dashed boundary).

**This Fix:**
- ✅ Added `ctx.save()` / `ctx.clip()` / `ctx.restore()` to renderCabinetIDNumbers
- ✅ Numbers now clip to raster bounds, just like other elements (borders, circle with X, etc.)
- ✅ Consistent with existing rendering behavior


v0.3.8.3 - January 28, 2026
---------------------------
BUG FIX: Duplicate/Paste ACTUALLY fixed now (backend issue)

**What Was Wrong:**
The frontend was sending all the settings, but the backend `add_layer` endpoint 
was ignoring them! It only copied basic geometry (columns, rows, size, offset).

**This Fix:**
- ✅ Backend now accepts ALL optional fields from POST request
- ✅ Cabinet ID settings, label settings, offset settings all preserved
- ✅ Duplicate and paste now truly copy everything

**Root Cause:**
The `add_layer` endpoint needs to handle two cases:
1. New blank layer (use defaults)
2. Duplicate/paste layer (use provided settings)

Now it checks the request data and applies any provided settings.


v0.3.8.2 - January 28, 2026
---------------------------
BUG FIX: Duplicate/Paste now preserves Cabinet ID settings

**Changes:**
- ✅ Fixed duplicate layer - now copies Cabinet ID style, position, and color
- ✅ Fixed paste layer - now copies Cabinet ID style, position, and color
- ✅ Simplified position labels: "Top Left" and "Center" (no size hints)

**What Was Broken:**
When duplicating or pasting a layer, the Cabinet ID settings would revert to default instead of copying from the source layer.


v0.3.8.1 - January 28, 2026
---------------------------
UI POLISH: Shortened labels, Colors panel fixed

**Changes:**
- ✅ Shortened numbering style labels for clarity:
  - "A1, B1, C1" (was "Column-Row (A1, B1, C1...)")
  - "A1, A2, A3" (was "Row-Column (A1, A2, A3...)")
  - "1,1  1,2  1,3" (was "Row,Col (1,1  1,2  1,3...)")
- ✅ Fixed Colors panel - now ONLY shows on Pixel Map tab
- ✅ Cabinet ID, Data Flow, Power tabs have no Colors section


v0.3.8 - January 28, 2026
-------------------------
FEATURE: Cabinet ID Numbering System

**New Cabinet ID Options:**
1. **Column-Row** (A1, B1, C1...) - Pixel Perfect Pro style
   - Reads top-to-bottom by columns
   - A1, A2, A3 down first column, then B1, B2, B3...

2. **Row-Column** (A1, A2, A3...) - Pixel Perfect Pro style
   - Reads left-to-right by rows
   - A1, A2, A3 across first row, then B1, B2, B3...

3. **Row,Col** (1,1  1,2  1,3...) - Video Walrus style
   - Comma notation: row number, column number
   - 1,1  1,2  1,3 across first row, then 2,1  2,2  2,3...

**Label Position Options:**
- **Top-Left (small)**: Compact labels in corner
- **Center (large)**: Centered labels (default)

**Per-Layer Settings:**
- Numbering style selection (radio buttons)
- Position selection (radio buttons)
- Font size control
- Label color picker
- Screen name toggle

**UI Cleanup:**
- Removed Colors section from Cabinet ID tab
- Removed Colors section from Data Flow tab
- Removed Colors section from Power tab
- Colors only visible on Pixel Map tab

**Future Feature Planned:**
Transform Mode - Resize screens by dragging corners
- ALL 4 corners controllable (just like offset display - TL, TR, BL, BR)
- Each corner controls resize with proper anchor point
- Live overlay showing "16x9 panels" during resize
- Activation: Cmd/Ctrl + Shift + click any corner
- Optional: Toggle button "Transform" at top toolbar with `T` shortcut
- Cursor changes per corner (nwse-resize, nesw-resize)


v0.3.7 - January 28, 2026
-------------------------
UX IMPROVEMENT: Default label settings

**Changes:**
- New layers now have "Size (px)" label enabled by default
- Shows pixel dimensions on startup (e.g., "1024x640")
- Provides immediate visual feedback on screen resolution

**Future Feature Planned:**
Transform Mode - Resize screens by dragging corners
- ALL 4 corners controllable (just like offset display - TL, TR, BL, BR)
- Each corner controls resize with proper anchor point
- Live overlay showing "16x9 panels" during resize
- Activation: Cmd/Ctrl + Shift + click any corner
- Optional: Toggle button "Transform" at top toolbar with `T` shortcut
- Cursor changes per corner (nwse-resize, nesw-resize)


v0.3.6 - January 28, 2026
-------------------------
CLEANUP: Removed all debug logging

**Changes:**
- Removed 76 console.log statements from app.js
- Removed 18 console.log statements from canvas.js
- Kept console.error for actual error handling
- Clean console output for production use

**Total Removed:** 94 debug statements

**Summary of v0.3.x Series:**
- v0.3.0-0.3.2: Fixed critical undo/redo bugs
- v0.3.3: Removed delete confirmation dialogs
- v0.3.4: Fixed DELETE endpoint to return full project
- v0.3.5: Photoshop-style layer ordering (newest on top)
- v0.3.5.4: Selection bounding box during Shift+Drag only
- v0.3.6: Debug cleanup (this version)


v0.3.5.4 - January 28, 2026
---------------------------
UX IMPROVEMENT: Bounding box only during Shift+Drag

**User Feedback:**
"I only want the bounding selection box to show up when I hold shift to move the screen. not all the time."

**Changes:**
- Bounding box now ONLY visible during Shift+Drag operations
- Uses `this.isDraggingLayer` flag to control visibility
- Clean canvas when not actively moving layers

**Test:**
1. Select a layer → no blue box ✅
2. Hold Shift + Drag layer → blue box appears ✅
3. Release mouse → blue box disappears ✅


v0.3.5.3 - January 28, 2026
---------------------------
FIXED: Corrected zoom reference bug

**What Was Wrong in v0.3.5.2:**
Used `this.camera.zoom` but this codebase uses `this.zoom` directly!

**This Version (v0.3.5.3):**
- ✅ CORRECTED zoom reference: `this.zoom` (not `this.camera.zoom`)
- ✅ Delete button selects layer below
- ✅ Selection bounding box (blue outline)
- ✅ Render works perfectly

**Changes:**
1. **Fixed Bounding Box** - Uses correct `this.zoom` reference
2. **Delete Button Fix** - Selects layer below (same as Delete key)

**Test:**
1. ✅ Can see raster (red border)
2. ✅ Can see screen layers
3. ✅ Delete button selects layer below
4. ✅ Blue box highlights selected layer
5. ✅ No TypeError on render


v0.3.5.2 - January 28, 2026 [BROKEN - zoom reference bug]
---------------------------
Used wrong zoom reference (this.camera.zoom instead of this.zoom).


v0.3.5.1 - January 28, 2026 [BROKEN - sed destroyed code]
---------------------------
Render broken by sed command.


v0.3.5 - January 28, 2026
-------------------------
PHOTOSHOP-STYLE LAYERS & DELETE BEHAVIOR

**Fix 1: Auto-Select Layer After Delete (Photoshop Behavior)**
When deleting with Delete key:
- Now selects the layer below the deleted one
- If deleting the bottom layer, selects the one above
- Uses same index position logic as Photoshop
- No more "nothing selected" after delete!

Code change:
```javascript
// Find index before delete, then select same index (or one above if at end)
const currentIndex = this.project.layers.findIndex(l => l.id === layerId);
// After delete:
const newIndex = Math.min(currentIndex, this.project.layers.length - 1);
this.currentLayer = this.project.layers[newIndex];
```

**Fix 2: Reverse Layer Order - Photoshop Style** 🎨
- Newest layers now appear at the TOP
- Just like Photoshop layers panel
- Makes more sense visually
- Backend array order unchanged (for Z-order consistency)

Code change:
```javascript
const reversedLayers = [...this.project.layers].reverse();
reversedLayers.forEach(layer => { /* render */ });
```

**Test:**
1. ✅ Create 3 layers → Newest appears at top of list
2. ✅ Delete middle layer with Delete key → Layer below auto-selects
3. ✅ Delete bottom layer → Top layer auto-selects

**Note:** Screens panel will move to bottom-right in future for preset system!


v0.3.4 - January 28, 2026
-------------------------
THE REAL FIX - Backend DELETE Endpoint

**Root Cause Found from Debug Logs:**
```
[Log] Project keys: – ["status"] (1)
[Log] Project.layers: – undefined
```

The DELETE layer endpoint was returning `{status: "success"}` instead of the full project!

**Fixes Applied:**
1. **Backend DELETE /api/layer/<id>**: Now returns full project
   ```python
   return jsonify(current_project)  # Not {status: 'success'}
   ```

2. **Frontend deleteLayer()**: Now uses returned project
   ```javascript
   .then(res => res.json())
   .then(project => {
     this.project = project;  // Use returned project
   ```

3. **Frontend deleteCurrentLayer()**: Already fixed in v0.3.2

**Why This Fixes Everything:**
- Delete key: ✅ Gets full project back, layers render correctly
- Trash button: ✅ Gets full project back, layers render correctly  
- No more `{status: "success"}` breaking the UI
- Undo/redo: ✅ Already working perfectly

**Test:**
1. Press Delete key → Layer deleted, UI updates ✅
2. Click trash button → Layer deleted, UI updates ✅
3. Press Cmd+Z → Deletion undone ✅

This should be THE fix!


v0.3.3.1 DEBUG - January 27, 2026
----------------------------------
DEBUG BUILD - Delete Key Issue Investigation

Added extensive console logging to track down the delete key freeze issue:

**Debug Logging Added:**
- `deleteCurrentLayer()`: Full lifecycle tracking
  - Entry conditions
  - Layer ID being deleted
  - API response details
  - Project structure after delete
  - State save completion
  - Error handling
  
- `updateUI()`: Execution flow tracking
  - Project state verification
  - renderLayers call
  - Canvas render call
  
- `renderLayers()`: Project validation
  - this.project existence
  - this.project.layers array
  - Layer count

**Issue to Debug:**
- Delete key removes layers from UI
- Layers remain visible in canvas
- UI freezes until Cmd+Z
- Need to see what the DELETE API is returning

**Test Instructions:**
1. Open browser console
2. Select a layer
3. Press Delete key
4. Send ALL console output from "=== DELETE CURRENT LAYER ===" onwards


v0.3.3 - January 27, 2026
-------------------------
REMOVED DELETE CONFIRMATIONS - Undo/Redo Makes Them Unnecessary

Since undo/redo is working perfectly, confirmation dialogs are no longer needed!

Changes:
- **Removed delete confirmation dialogs** from both:
  - Delete key (Delete/Backspace)
  - Trash button on layers
- **Just press Delete and undo if needed!**
- Much faster workflow
- Still protects against deleting last layer

Benefits:
- Faster deletion (no dialog interruption)
- Photoshop-style workflow
- Trust undo/redo system
- Cleaner user experience

Test:
1. ✅ Press Delete → Layer deleted immediately
2. ✅ Press Cmd+Z → Layer restored
3. ✅ Click trash button → Layer deleted immediately
4. ✅ Try to delete last layer → Shows alert (still protected)


v0.3.2 - January 27, 2026
-------------------------
CRITICAL FIXES - All Three Bugs

**Bug 1: Move Layer Now Saves to History** ✅
Problem: Moving a layer didn't call saveState(), so undo had nothing to undo
Fix: Modified updateLayer() to accept saveHistory parameter
- canvas.js now calls: `updateLayer(true, 'Move Layer')`
- Saves state BEFORE layer move is sent to backend
- Now you can undo/redo layer moves!

**Bug 2: Backend Returns Full Project** ✅
Problem: PUT /api/project returned `{status: "success"}` instead of project
- Frontend tried to use `data.layers` but data was just `{status}`
- Caused: `TypeError: undefined is not an object (evaluating 'this.project.layers.forEach')`
Fix: Backend now returns full project:
```python
return jsonify(current_project)  # Not {status: 'success'}
```

**Bug 3: Delete + Enter Fixed with Flag** ✅
Problem: Pressing Enter in delete dialog might retrigger delete handler
Fix: Added `deletionInProgress` flag
- Set to `true` when delete starts
- Prevents re-entry: `if (!this.currentLayer || this.deletionInProgress) return`
- Cleared in `.finally()` block after delete completes

**Test All Three:**
1. ✅ Move a layer (Shift+Drag), then Undo - layer should move back
2. ✅ Duplicate twice, Undo once - only ONE duplicate removed
3. ✅ Press Delete, then Enter - layer deletes correctly

Debugging logs still active for troubleshooting!


v0.3.1 - January 27, 2026
-------------------------
CRITICAL FIX: Undo/Redo Timing

**Problem Found:**
Your log showed:
```
Duplicate #1: saves 1 layer
Duplicate #2: saves 2 layers  
Undo once: restores 1 layer (BOTH duplicates gone!)
```

**Root Cause:**
`saveState()` was called BEFORE async operations completed!

Flow was:
```javascript
// WRONG:
duplicateLayer() {
  saveState('Duplicate');  // Saves OLD state (1 layer)
  fetch().then(() => {
    this.project.layers.push(newLayer);  // Now 2 layers
  });
}

// Result: Saved state has 1 layer, current has 2
// Undo restores 1 layer = both duplicates gone!
```

**The Fix:**
Move `saveState()` to AFTER async operations complete:

```javascript
// CORRECT:
duplicateLayer() {
  fetch().then(() => {
    this.project.layers.push(newLayer);  // Now 2 layers
    saveState('Duplicate');  // Saves NEW state (2 layers)
  });
}

// Result: Saved state has 2 layers
// Undo restores 2 layers = only last duplicate removed!
```

**Changes:**
- `duplicateLayer()`: saveState moved to end of .then()
- `pasteLayer()`: saveState moved to end of .then()
- `deleteCurrentLayer()`: saveState moved to end of .then()

**Test Flow:**
```
Start: 1 layer [history: initial(1)]
Duplicate: 2 layers [history: initial(1), dup1(2)]
Duplicate: 3 layers [history: initial(1), dup1(2), dup2(3)]
Undo: 2 layers [restores dup1(2)] ✅
Undo: 1 layer [restores initial(1)] ✅
Redo: 2 layers [restores dup1(2)] ✅
Redo: 3 layers [restores dup2(3)] ✅
```


v0.3.0 - January 27, 2026
-------------------------
MAJOR BUG FIXES - Undo/Redo & Delete

**Bug 1: Undo/Redo Now Working** ✅
Problem: historyIndex started at -1, so after first action (index 0), couldn't undo because we need index > 0

Root Cause:
```
Initial: historyIndex = -1, history = []
After paste: historyIndex = 0, history = [paste]
Try undo: Need historyIndex > 0... but it's 0! ❌
```

Solution:
- Save initial project state when app loads
- Now we have: history = [initial, action]
- Can undo from action back to initial ✅

Flow:
```
On load: historyIndex = 0, history = [initial]
After paste: historyIndex = 1, history = [initial, paste]
Press undo: historyIndex = 0, restore [initial] ✅
Press redo: historyIndex = 1, restore [paste] ✅
```

**Bug 2: Delete + Enter Fixed** ✅
Problem: Pressing Delete key, then Enter in confirmation dialog caused screen layer list to clear but layers stayed on canvas

Root Cause:
- Delete key handler triggers while confirmation dialog is open
- Enter key might re-trigger handler or cause race condition
- currentLayer still set during confirmation

Solution:
- Clear currentLayer BEFORE showing confirmation dialog
- Prevents re-trigger during confirmation
- Restore currentLayer if user clicks Cancel
- Only clears if user confirms

Code Changes:
```javascript
// Before:
if (confirm("Delete?")) {
  delete layer; // currentLayer still set during dialog!
}

// After:
const temp = currentLayer;
currentLayer = null; // Clear BEFORE dialog
if (confirm("Delete?")) {
  delete layer;
} else {
  currentLayer = temp; // Restore if cancelled
}
```

Debug Logging:
- Kept all console logging from v0.2.9.1
- Helps diagnose future undo/redo issues
- Shows saveState, undo, redo with full details

Test:
1. ✅ Load app - should see "Saving initial project state"
2. ✅ Duplicate layer (Cmd+J)
3. ✅ Undo (Cmd+Z) - duplicate disappears
4. ✅ Redo (Cmd+Shift+Z) - duplicate reappears
5. ✅ Press Delete, then Enter - layer deleted correctly


v0.2.9.1 - January 27, 2026
---------------------------
DEBUG BUILD - Undo/Redo Troubleshooting

Added extensive console logging to diagnose undo/redo issues:

**saveState() logging:**
- Action name
- Current historyIndex
- History length (before/after)
- Number of layers in saved state
- Whether future states were removed

**undo() logging:**
- Current historyIndex
- History length
- Can undo? (boolean check)
- Target index being restored
- Action being restored
- Number of layers in restored state
- Current layer name after restore
- Backend sync status and response
- Error messages if sync fails

**redo() logging:**
- Current historyIndex
- History length
- Can redo? (boolean check)
- Target index being restored
- Action being restored
- Number of layers in restored state
- Current layer name after restore
- Backend sync status and response
- Error messages if sync fails

**How to debug:**
1. Open browser console (F12)
2. Perform an action (duplicate, delete, etc.)
3. Look for "=== SAVE STATE ===" in console
4. Press Cmd+Z to undo
5. Look for "=== UNDO CALLED ===" in console
6. Share the console output so we can see what's happening!

**What to look for:**
- Does saveState get called?
- What's the historyIndex before/after?
- Does undo/redo get triggered?
- Are there any errors in backend sync?
- How many layers are in the restored state?


v0.2.9 - January 27, 2026
-------------------------
CRITICAL FIX: Undo/Redo Now Working

Fixes:
- **Undo/Redo now syncs with backend**
- Problem: Undo/redo only restored frontend state, backend kept old state
- Fix: Added PUT /api/project endpoint to restore full project state
- Both undo() and redo() now call backend to sync

What Was Wrong:
```javascript
// OLD (broken):
this.project = restored_state;
this.updateUI(); // Only updates display, backend unchanged

// NEW (working):
this.project = restored_state;
fetch('/api/project', { method: 'PUT', body: this.project })
  .then(() => this.updateUI()); // Backend synced!
```

Backend Changes:
- Added `PUT /api/project`: Restores entire project state
- Emits `project_updated` via WebSocket
- Used exclusively by undo/redo operations

Test:
1. Create a layer
2. Duplicate it (Cmd+J)
3. Undo (Cmd+Z) - layer should disappear ✅
4. Redo (Cmd+Shift+Z) - layer should reappear ✅


v0.2.8 - January 27, 2026
-------------------------
PHOTOSHOP-STANDARD SHORTCUTS

Changes:
- **Cmd+J / Ctrl+J**: Duplicate layer (Photoshop standard)
- **Removed Cmd+D**: Was non-standard, now uses Cmd+J
- **Removed Cmd+R**: Was non-standard for redo

Now 100% Photoshop-Compatible:
- **Cmd+Z / Ctrl+Z**: Undo ✅
- **Cmd+Shift+Z / Ctrl+Shift+Z**: Redo ✅
- **Cmd+C / Ctrl+C**: Copy ✅
- **Cmd+V / Ctrl+V**: Paste ✅
- **Cmd+J / Ctrl+J**: Duplicate layer ✅
- **Delete/Backspace**: Delete ✅
- **Space+Drag**: Pan (Hand tool) ✅

Complete Shortcut List (Mac):
- **Cmd+Z**: Undo
- **Cmd+Shift+Z**: Redo
- **Cmd+C**: Copy layer (not when typing)
- **Cmd+V**: Paste layer (not when typing)
- **Cmd+J**: Duplicate layer (not when typing)
- **Delete/Backspace**: Delete layer (not when typing)
- **Space+Drag**: Pan canvas (not when typing)
- **Alt+Click**: Hide/unhide panel
- **Shift+Drag**: Move layer

Complete Shortcut List (Windows/Linux):
- **Ctrl+Z**: Undo
- **Ctrl+Shift+Z**: Redo
- **Ctrl+C**: Copy layer (not when typing)
- **Ctrl+V**: Paste layer (not when typing)
- **Ctrl+J**: Duplicate layer (not when typing)
- **Delete/Backspace**: Delete layer (not when typing)
- **Space+Drag**: Pan canvas (not when typing)
- **Alt+Click**: Hide/unhide panel
- **Shift+Drag**: Move layer

Photoshop Muscle Memory:
If you're a Photoshop user, all shortcuts will feel natural!


v0.2.7 - January 27, 2026
-------------------------
KEYBOARD SHORTCUTS ENHANCED

New Shortcuts:
- **Cmd+D / Ctrl+D**: Duplicate current layer
- **Cmd+R / Ctrl+R**: Redo (alternative to Cmd+Shift+Z)

Browser Default Prevention:
- **Cmd+D**: Prevents "Add Bookmark" dialog
- **Cmd+R**: Prevents page reload
- All shortcuts use `e.preventDefault()` to block browser actions

Complete Shortcut List (Mac):
- **Cmd+Z**: Undo
- **Cmd+Shift+Z**: Redo
- **Cmd+R**: Redo (alternative)
- **Cmd+C**: Copy layer (not when typing)
- **Cmd+V**: Paste layer (not when typing)
- **Cmd+D**: Duplicate layer (not when typing)
- **Delete/Backspace**: Delete layer (not when typing)
- **Space+Drag**: Pan canvas (not when typing)
- **Alt+Click**: Hide/unhide panel
- **Shift+Drag**: Move layer

Complete Shortcut List (Windows/Linux):
- **Ctrl+Z**: Undo
- **Ctrl+Shift+Z**: Redo
- **Ctrl+R**: Redo (alternative)
- **Ctrl+C**: Copy layer (not when typing)
- **Ctrl+V**: Paste layer (not when typing)
- **Ctrl+D**: Duplicate layer (not when typing)
- **Delete/Backspace**: Delete layer (not when typing)
- **Space+Drag**: Pan canvas (not when typing)
- **Alt+Click**: Hide/unhide panel
- **Shift+Drag**: Move layer

Technical:
- All shortcuts check `e.metaKey || e.ctrlKey` for cross-platform
- Cmd on Mac = metaKey, Ctrl on Windows/Linux = ctrlKey
- `isTyping` check prevents shortcuts when in text fields
- `e.preventDefault()` blocks browser defaults


v0.2.6 - January 27, 2026
-------------------------
UI & NAMING IMPROVEMENTS

Fixes:
- **Layer buttons now visible without scrolling**
- Reduced layer name input width: 120px → 80px
- Icons (👁 📋 🗑) now fit in visible area
- No more horizontal scrolling needed

- **Smart duplicate naming**
- Increments numbers instead of adding "(Copy)"
- Examples:
  - "Screen1" → "Screen2"
  - "Screen12" → "Screen13"
  - "Nvidia" → "Nvidia 1"
  - "Nvidia 5" → "Nvidia 6"

Logic:
- If name ends with number: increment it
- If name has no number: add " 1"
- Works for both duplicate button and copy/paste

Examples:
```
Screen1 → duplicate → Screen2
Screen2 → duplicate → Screen3
Nvidia → duplicate → Nvidia 1
Nvidia 1 → duplicate → Nvidia 2
Main12 → duplicate → Main13
Test → copy/paste → Test 1
```


v0.2.5 - January 27, 2026
-------------------------
BUG FIXES

Fixes:
- **Fixed duplicate layer button** (📋)
- Changed endpoint from `/api/layer` to `/api/layer/add`
- Duplicate now works correctly

- **Fixed copy/paste** (Ctrl+C / Ctrl+V)
- Changed endpoint from `/api/layer` to `/api/layer/add`
- Copy/paste now works correctly
- Console logs "Copied layer: [name]"

- **All center labels now centered together**
- Screen name + other labels centered as a group
- Previously: Only other labels were centered, screen name above
- Now: All labels (screen name + dimensions/sizes) centered together
- Info label still at bottom (unchanged)

Visual Improvement:
- Labels now look properly grouped and centered
- Screen name and dimension labels form a cohesive unit
- Better visual hierarchy

Note:
- Trash can (🗑) already present in layer list (was never removed)


v0.2.4 - January 27, 2026
-------------------------
NEW FEATURE: Decimal/Fractional Inches Toggle

Features:
- **Decimal inches by default**: e.g., "2' 2.5""
- **Fractional toggle checkbox**: "Fractions" next to "Layer Size (ft)"
- **Fractional mode**: e.g., "2' 2 1/2"" or "2' 2 7/8""

Fractional Precision:
- Rounds to nearest 1/16th inch
- Auto-simplifies common fractions:
  - 1/2, 1/4, 3/4
  - 1/8, 3/8, 5/8, 7/8
  - Shows as "/16" for others (e.g., 5/16, 9/16)
- Omits fractions smaller than 1/16"

Examples:
- Decimal: "W 2' 2.5" X H 3' 10.8""
- Fractional: "W 2' 2 1/2" X H 3' 10 7/8""

UI Changes:
- Added "Fractions" checkbox next to "Layer Size (ft)"
- Checkbox styled smaller (11px gray)
- Per-layer setting (saved with layer)

Technical:
- New property: `useFractionalInches` (boolean)
- Added to backend layer update keys
- Frontend toggles between decimal (toFixed(1)) and fraction conversion
- Fraction conversion function handles all common fractions


v0.2.3 - January 27, 2026
-------------------------
BUG FIXES

Fixes:
- **Zoom sensitivity reduced another 50%**
- Now 2.5% per scroll (was 5%, originally 10%)
- Zoom factor: 1.025/0.975 (was 1.05/0.95)
- Much smoother, gentler zoom control

- **Fixed negative feet/inches calculation**
- Bug: Complex nested calculation caused negative fractional inches
- Example bug: "12.-41"" instead of "12.59""
- Root cause: `Math.round((((widthFt - widthFtIn) * 12) - widthIn) * 100)`
  - When widthIn was rounded up, the subtraction went negative

New Calculation (Simplified):
```javascript
const widthFtTotal = Math.floor(widthFt);           // e.g., 2
const widthInchesDecimal = (widthFt - widthFtTotal) * 12;  // e.g., 2.456
const widthInTotal = Math.floor(widthInchesDecimal); // e.g., 2
const widthInFraction = Math.round((widthInchesDecimal - widthInTotal) * 100); // e.g., 46
// Result: "2' 2.46""
```

Test Case (84px × 84px, 8col × 14row):
- Total: 672px × 1176px
- Meters: 0.672m × 1.176m
- Feet: 2.20ft × 3.86ft
- ✅ Now shows: "W 2' 2.46" X H 3' 10.31""
- ❌ Was showing: "W 2' 2.-41" X H 3' 10.-69""


v0.2.2 - January 27, 2026
-------------------------
IMPROVEMENTS

Fixes:
- **Raster width/height inputs now support math expressions**
- Changed toolbar raster inputs from `type="number"` to `type="text"`
- Can now type "1920*2" or "1080+400" in raster fields

- **Labels now clipped to raster bounds**
- Added canvas clipping to renderLayerLabels (same as CircleWithX)
- Labels no longer render outside the raster boundary
- Cleaner visual presentation

- **Zoom sensitivity reduced by 50%**
- Changed from 10% per scroll to 5% per scroll
- Zoom factor: 1.1/0.9 → 1.05/0.95
- Much smoother, less jarring trackpad zoom

Technical Changes:
- renderLayerLabels() now uses ctx.save(), ctx.clip(), ctx.restore()
- Clips to rect(0, 0, rasterWidth, rasterHeight) before rendering
- Zoom factor reduced from ±10% to ±5%


v0.2.1 - January 27, 2026
-------------------------
MATH EXPRESSIONS FIXED

Root Cause Found:
- **Input fields were `type="number"` which blocks asterisk (*) character!**
- HTML number inputs only allow: digits, decimal point, minus sign
- Math operators like *, +, /, () are blocked by the browser

The Fix:
- Changed to `type="text"` for fields that support math expressions:
  - Columns
  - Rows
  - Offset X
  - Offset Y
  - Cabinet Width
  - Cabinet Height

Now Working:
- ✅ Type "2*3" → evaluates to 6
- ✅ Type "10+5" → evaluates to 15
- ✅ Type "100/2" → evaluates to 50
- ✅ Type "128*4" → evaluates to 512

Kept as `type="number"` (no math needed):
- Raster width/height
- Panel dimensions (mm)
- Panel weight
- Font sizes
- Power settings

Removed:
- Debug logging from v0.2.0.3


v0.2.0.3 - January 27, 2026
---------------------------
DEBUG BUILD

Added:
- **Debug logging for asterisk (*) key**
- Console will log when you press * or Shift+8
- Shows: key, code, shiftKey, isTyping, activeElement, defaultPrevented

How to Debug:
1. Open browser console (F12)
2. Click in a text input field (like Columns)
3. Press Shift+8 (asterisk)
4. Check console output
5. Share the output so we can fix it!

Expected Output:
```
Asterisk key pressed: {
  key: "*",
  code: "Digit8",
  shiftKey: true,
  isTyping: true,
  activeElement: "INPUT",
  defaultPrevented: false
}
```

If `defaultPrevented: true`, something else is blocking it!


v0.2.0.2 - January 27, 2026
---------------------------
CRITICAL FIX

Fixes:
- **Delete/Backspace now only deletes layers when NOT typing in text fields**
- **Asterisk (*) and other keys now work in input fields**
- Keyboard shortcuts now check if user is typing before triggering

What Was Wrong:
- handleKeyDown was attached to `document` (global)
- Fired on ALL keyboard events, even when typing
- `e.preventDefault()` blocked asterisk from appearing in inputs

The Fix:
```javascript
const isTyping = document.activeElement.tagName === 'INPUT' || 
                document.activeElement.tagName === 'TEXTAREA' ||
                document.activeElement.isContentEditable;
```

Behavior Now:
- **Delete/Backspace**: Only deletes layer when NOT in text field ✅
- **Space**: Only pans canvas when NOT in text field ✅
- **Ctrl/Cmd+C/V**: Only copies/pastes layers when NOT in text field ✅
- **Ctrl/Cmd+Z**: Undo works everywhere (including text fields) ✅
- **Asterisk (*)**: Works normally in all text inputs ✅


v0.2.0.1 - January 27, 2026
---------------------------
CRITICAL FIX

Fixes:
- **Syntax error fixed in canvas.js**
- Removed leftover duplicate code from handleKeyDown function
- App now loads correctly (raster and screen visible again)

What Was Wrong:
- Lines 276-278 had orphaned code from incomplete edit
- Caused JavaScript parsing error
- Prevented entire app from loading

Math Expressions:
- Multiplication (e.g., "2*3") works correctly
- Function tested and verified working
- If you're seeing issues, please provide specific example


v0.2.0 - January 27, 2026
-------------------------
MAJOR FEATURE UPDATE

NEW FEATURES:

✅ Delete Key Functionality
- Press Delete or Backspace to remove selected layer
- Confirmation dialog: "Delete layer [name]?"
- Keyboard shortcut works when layer is selected

✅ Duplicate Layer Button
- New 📋 button in SCREENS panel next to visibility and delete
- Creates copy with "(Copy)" appended to name
- Duplicate placed at +50px offset from original
- All properties copied: colors, labels, settings, panels

✅ Undo/Redo System
- Ctrl+Z / Cmd+Z: Undo last action
- Ctrl+Shift+Z / Cmd+Shift+Z: Redo
- Tracks: Add Layer, Delete Layer, Duplicate Layer, Paste Layer
- History limit: 50 actions
- Console logs action name on undo/redo

✅ Copy/Paste Functionality
- Ctrl+C / Cmd+C: Copy selected layer to clipboard
- Ctrl+V / Cmd+V: Paste layer with "(Copy)" suffix
- Pasted layer offset by +50px from original
- Console logs "Copied layer: [name]"

TECHNICAL IMPLEMENTATION:

History System:
- `history[]` array stores project snapshots
- `historyIndex` tracks current position
- `saveState(action)` called before modifications
- Deep clones project state with JSON.parse/stringify
- Restores layer references on undo/redo

Keyboard Handlers (canvas.js):
- Delete/Backspace → `deleteCurrentLayer()`
- Ctrl/Cmd+Z → `undo()`
- Ctrl/Cmd+Shift+Z → `redo()`
- Ctrl/Cmd+C → `copyLayer()`
- Ctrl/Cmd+V → `pasteLayer()`

Clipboard:
- Stores complete layer data as JSON
- Preserved across operations until overwritten

UI Changes:
- Added duplicate button (📋) to layer controls
- Added tooltips to all layer control buttons
- Layer control buttons: 👁 (visibility), 📋 (duplicate), 🗑 (delete)


v0.1.8 - January 27, 2026
-------------------------
FEATURE IMPROVEMENT

Features:
- **Labels Fontsize max increased from 48 to 100**
- You can now make main labels much larger for big displays
- Info label stays at 14px (unaffected)

Updated TODO List:
- Added "QOL Features" section for quality of life improvements
- Info Label Font Size Slider (future feature)
- Top Menu Bar (future feature)
- Right-Click Context Menu (future feature)

Changes:
- Updated HTML input max="48" to max="100"
- TODO.txt reorganized with completed items and QOL section


v0.1.7.3 - January 27, 2026
---------------------------
BUG FIX (Corrected Understanding)

Fixes:
- **Info label now ACTUALLY independent of "Labels Fontsize" slider**
- Info label locked to 14px (does NOT change when you adjust the slider)
- Main labels (Screen Name, dimensions) still use the slider value

What User Actually Meant:
- NOT about zoom scaling (that was my misunderstanding in v0.1.7.2)
- About the "Labels Fontsize" slider in the UI
- When user changes slider 14→20, main labels should grow but info stays 14px

The Correct Fix:
```javascript
const fontSize = layer.labelsFontSize || 14;  // From slider
const infoFontSize = 14;                       // Always 14px
```

Reverted v0.1.7.2 Changes:
- Removed incorrect zoom compensation (14 / this.zoom)
- That was solving the wrong problem
- Info label now just uses constant 14, ignores slider


v0.1.7.2 - January 27, 2026
---------------------------
BUG FIX

Fixes:
- **Info label NOW ACTUALLY fixed to 14px screen size**
- Info label no longer scales with zoom
- Info label remains constant 14px regardless of zoom level

What Was Wrong in v0.1.7.1:
- Set infoFontSize = 14, but it was still inside the zoom transform
- Everything drawn inside `ctx.save()` / `ctx.restore()` scales with zoom
- Font size 14px at 100% zoom becomes 28px at 200% zoom

The Real Fix:
- **Compensate for zoom**: `infoFontSize = 14 / this.zoom`
- At 100% zoom: 14 / 1.0 = 14px world size → 14px screen size ✅
- At 200% zoom: 14 / 2.0 = 7px world size → 14px screen size ✅
- At 50% zoom: 14 / 0.5 = 28px world size → 14px screen size ✅

Technical:
- Info font: `const infoFontSize = 14 / this.zoom;`
- Info line height: `const infoLineHeight = infoFontSize + 4;`
- Updated all info label rendering to use these constants


v0.1.7.1 - January 27, 2026
---------------------------
CRITICAL FIX

Fixes:
- **Info label now locked to 14px font size**
- Info label no longer affected by "Labels Fontsize" slider
- Main labels (Screen Name, dimensions) use the slider value
- Info label (bottom text with stats) always uses 14px

- **Hidden panels no longer restored when moving layers**
- Root cause: offset_x/offset_y changes were regenerating ALL panels
- This wiped out hidden/blank states
- Now: offset changes only update panel positions
- Grid/size changes preserve hidden/blank states from old panels

What Was Happening:
1. User hides panels with Alt+click ✅
2. User moves layer with Shift+drag ✅
3. Backend receives offset_x/offset_y update ✅
4. Backend regenerates ALL panels (line 169) ❌
5. All panels reset to hidden=false ❌
6. Hidden panels reappear! ❌

Now Fixed:
- **offset_x/offset_y**: Only updates panel x/y positions (preserves states)
- **columns/rows/cabinet_width/cabinet_height**: Regenerates panels but preserves states
- Saves old_panel_states before regenerating
- Restores hidden/blank states after regenerating

Technical:
- Added separate `infoFontSize = 14` constant
- Added separate `infoLineHeight = infoFontSize + 4`
- Split update logic: offset-only vs full regeneration
- Preserve states using old_panel_states dictionary


v0.1.7 - January 27, 2026
-------------------------
IMPROVEMENTS & DEBUG

Features:
- **Aspect Ratio added to info label**
- Shows as "Aspect Ratio: 1.60 : 1" format
- Info label now shows: Columns/Rows • Cabinets • Resolution • Aspect Ratio • Weight

Debug:
- Added console logging for Ctrl+Shift+Click behavior
- Logs keyboard modifiers when Ctrl, Meta (Cmd), or Shift are pressed with click
- Check browser console (F12) to see what's happening with the hidden panel restoration

Changes:
- Info label format: "8 Columns X 5 Rows • 40 Cabinets Total / Resolution: 1024 X 640 • Aspect Ratio: 1.60 : 1 • Weight: 800.0 kg / 1763.7 lb"
- Aspect ratio calculated as: layerWidth / layerHeight
- Formatted to 2 decimal places

Notes for Future:
- Right-click context menu planned with: Duplicate, Copy, Paste, Unhide All
- Top menu bar planned with various functions
- Ctrl+Shift+Click currently restoring hidden panels - investigating cause


v0.1.6 - January 27, 2026
-------------------------
NEW FEATURE - Default Layer on Startup

Feature:
- **App now opens with a default layer already added and selected**
- No need to click "Add Screen" to start working
- Default layer: "Screen1", 8x5 grid, 128px panels, at (0,0)

Changes:
- Added `initialize_default_layer()` function in app.py
- Called on page load (index route) if project has no layers
- Called when creating new project
- Frontend auto-selects first layer on load
- Both `loadProject()` and `createNewProject()` now auto-select first layer

Behavior:
- First time opening app: Screen1 already present and selected
- Click "New": Creates new project with Screen1 already present and selected
- Start editing immediately without clicking "Add Screen"


v0.1.5.3 - January 27, 2026
---------------------------
CRITICAL FIX

Circle with X Toggle - FINALLY FIXED:
- **Root cause found**: Backend wasn't saving `show_circle_with_x` property
- Added `'show_circle_with_x'` to allowed update keys in app.py line 142
- Backend now properly persists the toggle state
- Toggle should now work perfectly!

What was happening:
1. Frontend set `show_circle_with_x = false` ✅
2. Frontend called immediate render (circle disappeared) ✅
3. Frontend sent update to backend ✅
4. Backend received it but IGNORED it (not in allowed keys list) ❌
5. Backend sent layer back WITHOUT the property ❌
6. Frontend received layer, saw undefined, defaulted to TRUE ❌
7. Circle reappeared! ❌

Now fixed:
- Backend now accepts and saves `show_circle_with_x` in the update
- Toggle state persists properly


v0.1.5.2 - January 27, 2026
---------------------------
MAJOR FIXES

Panel Modes Simplified:
- **Removed "blank" mode** - No more grey/dimmed panels
- **Only two states now**: Normal (colored) and Hidden (ghost outline with Alt+click)
- Right-click/Ctrl+click no longer toggles blank mode

Circle with X Toggle Fixed:
- **Added immediate render** after updating layer properties
- Toggle now works instantly without waiting for server response
- `window.canvasRenderer.render()` called immediately after saving

Right-Click Behavior Changed:
- Right-click now shows console message: "Context menu coming soon"
- Prepared for future context menu with: Duplicate, Copy, Paste
- No longer toggles blank mode

Changes:
- Removed all `if (panel.blank)` checks from rendering functions
- All panels now render with normal checkerboard colors
- renderPixelMap, renderCabinetID, renderDataFlow, renderPower updated
- Cabinet ID numbers no longer skip blank panels (only hidden)
- handleContextMenu() now placeholder for future menu

Debug Notes:
- Console logs confirmed toggle saves correctly (true/false)
- Issue was lack of immediate render - now fixed
- Blank mode completely removed from codebase


v0.1.5.1 - January 27, 2026
---------------------------
BUG FIXES & DEBUG

Fixes:
- **Screen name now renders ON TOP of Circle/X** - Moved circle rendering before labels
- Added debug logging for Circle with X toggle (console.log statements)
- Investigating why toggle doesn't work despite correct code structure

Debug Info:
- Added console logging: "updateLayerFromInputs - showCircleWithX: [value]"
- Added console logging: "Saved to layer - show_circle_with_x: [value]"
- Check browser console (F12) when toggling checkbox

Mac-Specific Behavior Noted:
- Ctrl+click on Mac = right-click (context menu) = toggles panel blank/active
- This is browser default behavior, not a bug
- User reported: Shift+Ctrl restores hidden panels (investigating)
- User wants: Shift+Option for restore instead

Changes:
- Circle/X renders BEFORE labels (line 297-300 moved to before line 304)
- Labels now appear on top of test pattern

Still investigating:
- Why Circle/X toggle doesn't hide/show despite correct event listeners
- Shift+Ctrl behavior (may be browser default on Mac)


v0.1.5 - January 27, 2026
-------------------------
BUG FIXES

Critical Fixes:
- **Borders and Circle with X now default to ON** - Fixed checkbox logic that was defaulting to unchecked
- **Circle size reduced** to 40% of smaller dimension (matches Pixel Perfect Pro reference)
- **Circle and X clipped to raster bounds** - No longer draws outside the red raster rectangle
- **Toggle now works properly** - Unchecking Circle with X immediately hides it

Root Cause:
- Checkbox loading used `|| false` which made undefined values default to unchecked
- Changed to: `!== undefined ? value : true` so new layers default to checked

Changes:
- Circle radius: `Math.min(layerWidth, layerHeight) * 0.40` (was 0.50)
- Added clipping region: `ctx.rect(0, 0, rasterWidth, rasterHeight)` with `ctx.clip()`
- Used `ctx.save()` and `ctx.restore()` to isolate clipping region
- Both checkboxes now properly reflect backend default values (true)

Technical Details:
- loadLayerToInputs() now handles undefined with ternary operator
- renderCircleWithX() wraps drawing in save/clip/restore for bounds enforcement
- Clipping prevents any part of circle or X from appearing outside raster


v0.1.4 - January 27, 2026
-------------------------
FEATURES & IMPROVEMENTS

New Features:
- **Screen Name Label Styling**: Screen name now displays with WHITE background and BLACK text, positioned separately above other labels
- **Circle with X Test Pattern**: Test pattern overlay showing ONE circle and ONE X across the ENTIRE layer (not per panel)
- **Borders Default ON**: New layers now have panel borders enabled by default

Circle with X Details:
- **Purpose**: Aspect ratio test pattern (like Pixel Perfect Pro)
- **ONE circle** centered on the layer, radius = min(width, height) / 2
- **ONE X** drawn corner-to-corner of entire layer (diagonal lines)
- Perfect circle maintains aspect ratio regardless of layer dimensions
- Only visible in Pixel Map view mode
- Default: ON for new layers

Changes:
- Screen name (layer name) renders separately from other labels with distinctive white background
- Screen name positioned 5px above other center labels when both are present
- Circle with X rendered after all panels and labels (on top)
- Uses same border color and lineWidth as panel borders
- Removed debug console.log statements from Alt+click handler

UI Updates:
- Added "Circle with X" checkbox in Pixel Map tab below "Show Panel Borders"
- Screen name uses bold font, white background (rgba(255,255,255,0.9)), black text

Technical:
- Added `show_circle_with_x` property to layer data structure
- New `renderCircleWithX(layer)` function renders test pattern for entire layer
- Circle uses `ctx.arc()` with radius based on smallest layer dimension
- X draws from (offset_x, offset_y) to (offset_x + width, offset_y + height) and opposite corners
- Default values in create_layer(): `show_panel_borders: True`, `show_circle_with_x: True`


v0.1.3 - January 27, 2026
-------------------------
CRITICAL FIXES

Fixes:
- Hidden panels now render as clickable ghost outlines (dashed semi-transparent border)
- **Alt+click on ghost outline now works - toggles panel back to visible**
- Fixed `getPanelAt()` to include hidden panels in click detection (was skipping them)
- **Panel borders now scale WITH zoom (like text) - not constant screen size**
- At 100% zoom: 2px borders; at 200% zoom: 4px borders; at 10% zoom: 0.2px borders (nearly invisible)
- Fixed Data Flow arrows to render in world space (was using screen coordinates incorrectly)
- Fixed Power lightning bolts to render in world space

Bug Fix:
- **Root cause**: `getPanelAt()` had `if (panel.hidden) continue;` which prevented hidden panels from being detected on click
- **Solution**: Removed the filter so hidden panels are clickable
- Added debug logging to Alt+click handler to verify toggle behavior

Changes:
- All render functions (renderPixelMap, renderCabinetID, renderDataFlow, renderPower) now check for hidden panels first
- Hidden panels render with: `strokeStyle: rgba(255,255,255,0.3)`, `lineWidth: 1`, dashed pattern `[5, 5]`
- Arrow and lightning bolt symbols now use world coordinates and scale properly
- **Border lineWidth changed from `2 / this.zoom` to `2` - borders now scale with content like text does**
- Raster boundary also scales with zoom: `lineWidth: 2`, `setLineDash: [10, 5]` (no division by zoom)

Technical:
- Removed `if (panel.hidden) return;` filter in render loops - all panels render now
- Removed `if (panel.hidden) continue;` filter in getPanelAt() - all panels clickable now
- Ghost outline: semi-transparent white dashed border that scales with zoom
- Symbols (arrows, lightning) positioned at world coordinates (panel.x + width/2)
- All lineWidth values now use absolute numbers (2, 1) instead of divided by zoom
- This makes borders "painted onto" the content, scaling proportionally like text


v0.1.2 - January 27, 2026
-------------------------
IMPROVEMENTS & FIXES

Features:
- Info label now displays as single row for more compact display
- Alt+click now properly toggles panel visibility (hide/unhide)
- New button now creates a fresh blank project instead of reloading

Fixes:
- New button properly clears project and resets UI
- Panel borders maintain constant 2px screen width regardless of zoom
- Border rendering uses correct inverse zoom calculation (2 / zoom)

Changes:
- Info label consolidated from 2 rows to 1 row: "8 Columns X 5 Rows • 40 Cabinets Total / Resolution: 1024 X 640 • Weight: 800.0 kg / 1763.7 lb"
- Added /api/project/new endpoint for creating new projects
- Added createNewProject() method in frontend


v0.1.1 - January 27, 2026
-------------------------
BUG FIX RELEASE

Fixes:
- Fixed text scaling to work like professional LED software (Pixel Perfect Pro)
- Text now scales proportionally with zoom level (maintains visual relationship to panels)
- At 100% zoom (1:1), text renders at exact pixel size specified (e.g., 14px)
- At 200% zoom, text scales to 28px on screen; at 50% zoom, scales to 7px
- All labels, numbers, and offsets now render in world space (inside transform)
- Text appears "painted onto" the LED wall rather than floating above it

How it works:
- Labels, offsets, and cabinet IDs render BEFORE ctx.restore() 
- Canvas transform (zoom + pan) applied to all text rendering
- Text coordinates use world space (no screen space conversion)
- Result: Text scales proportionally with content like in Pixel Perfect Pro

Technical changes:
- Moved renderLayerLabels(), renderLayerOffsets(), renderCabinetIDNumbers() before ctx.restore()
- Changed all text coordinates from screen space (x * zoom + panX) to world space (x)
- Text now scales with zoom transform matrix automatically


v0.1 - January 27, 2026
-----------------------
BASELINE RELEASE

Features:
- Layer-based LED panel layout system
- Grid-based panel generation
- Magnetic snapping to edges and other layers (20px threshold)
- Multiple view modes: Pixel Map, Data Flow, Power, Test Pattern
- Spacebar-based canvas panning
- Zoom controls (mouse wheel)
- Layer visibility toggle
- Panel operations: hide (Alt+click), blank (right-click)
- Customizable labels per layer (name, size in px/m/ft, info)
- Corner offset display (TL, TR, BL, BR)
- Project save/load functionality
- Export capabilities

Fixes in v0.1:
- Fixed magnetic snapping on mouse release (elements no longer jump after drag)
- Fixed offset coordinate math to account for zero-indexed pixels
  (e.g., 1024px wide screen now shows 0-1023 instead of 0-1024)

Technical:
- Python Flask backend
- HTML5 Canvas frontend
- WebSocket real-time communication
- Grid-based architecture for LED panels

Version Numbering:
- Major releases: 1.0, 2.0, 3.0
- Major incremental: 1.1, 1.2, 1.3
- Minor fixes: 1.1.1, 1.1.2, 1.1.3

-------------------------
CRITICAL FIXES

Fixes:
- Hidden panels now render as clickable ghost outlines (dashed semi-transparent border)
- **Alt+click on ghost outline now works - toggles panel back to visible**
- Fixed `getPanelAt()` to include hidden panels in click detection (was skipping them)
- **Panel borders now scale WITH zoom (like text) - not constant screen size**
- At 100% zoom: 2px borders; at 200% zoom: 4px borders; at 10% zoom: 0.2px borders (nearly invisible)
- Fixed Data Flow arrows to render in world space (was using screen coordinates incorrectly)
- Fixed Power lightning bolts to render in world space

Bug Fix:
- **Root cause**: `getPanelAt()` had `if (panel.hidden) continue;` which prevented hidden panels from being detected on click
- **Solution**: Removed the filter so hidden panels are clickable
- Added debug logging to Alt+click handler to verify toggle behavior

Changes:
- All render functions (renderPixelMap, renderCabinetID, renderDataFlow, renderPower) now check for hidden panels first
- Hidden panels render with: `strokeStyle: rgba(255,255,255,0.3)`, `lineWidth: 1`, dashed pattern `[5, 5]`
- Arrow and lightning bolt symbols now use world coordinates and scale properly
- **Border lineWidth changed from `2 / this.zoom` to `2` - borders now scale with content like text does**
- Raster boundary also scales with zoom: `lineWidth: 2`, `setLineDash: [10, 5]` (no division by zoom)

Technical:
- Removed `if (panel.hidden) return;` filter in render loops - all panels render now
- Removed `if (panel.hidden) continue;` filter in getPanelAt() - all panels clickable now
- Ghost outline: semi-transparent white dashed border that scales with zoom
- Symbols (arrows, lightning) positioned at world coordinates (panel.x + width/2)
- All lineWidth values now use absolute numbers (2, 1) instead of divided by zoom
- This makes borders "painted onto" the content, scaling proportionally like text


v0.1.2 - January 27, 2026
-------------------------
IMPROVEMENTS & FIXES

Features:
- Info label now displays as single row for more compact display
- Alt+click now properly toggles panel visibility (hide/unhide)
- New button now creates a fresh blank project instead of reloading

Fixes:
- New button properly clears project and resets UI
- Panel borders maintain constant 2px screen width regardless of zoom
- Border rendering uses correct inverse zoom calculation (2 / zoom)

Changes:
- Info label consolidated from 2 rows to 1 row: "8 Columns X 5 Rows • 40 Cabinets Total / Resolution: 1024 X 640 • Weight: 800.0 kg / 1763.7 lb"
- Added /api/project/new endpoint for creating new projects
- Added createNewProject() method in frontend


v0.1.1 - January 27, 2026
-------------------------
BUG FIX RELEASE

Fixes:
- Fixed text scaling to work like professional LED software (Pixel Perfect Pro)
- Text now scales proportionally with zoom level (maintains visual relationship to panels)
- At 100% zoom (1:1), text renders at exact pixel size specified (e.g., 14px)
- At 200% zoom, text scales to 28px on screen; at 50% zoom, scales to 7px
- All labels, numbers, and offsets now render in world space (inside transform)
- Text appears "painted onto" the LED wall rather than floating above it

How it works:
- Labels, offsets, and cabinet IDs render BEFORE ctx.restore() 
- Canvas transform (zoom + pan) applied to all text rendering
- Text coordinates use world space (no screen space conversion)
- Result: Text scales proportionally with content like in Pixel Perfect Pro

Technical changes:
- Moved renderLayerLabels(), renderLayerOffsets(), renderCabinetIDNumbers() before ctx.restore()
- Changed all text coordinates from screen space (x * zoom + panX) to world space (x)
- Text now scales with zoom transform matrix automatically


v0.1 - January 27, 2026
-----------------------
BASELINE RELEASE

Features:
- Layer-based LED panel layout system
- Grid-based panel generation
- Magnetic snapping to edges and other layers (20px threshold)
- Multiple view modes: Pixel Map, Data Flow, Power, Test Pattern
- Spacebar-based canvas panning
- Zoom controls (mouse wheel)
- Layer visibility toggle
- Panel operations: hide (Alt+click), blank (right-click)
- Customizable labels per layer (name, size in px/m/ft, info)
- Corner offset display (TL, TR, BL, BR)
- Project save/load functionality
- Export capabilities

Fixes in v0.1:
- Fixed magnetic snapping on mouse release (elements no longer jump after drag)
- Fixed offset coordinate math to account for zero-indexed pixels
  (e.g., 1024px wide screen now shows 0-1023 instead of 0-1024)

Technical:
- Python Flask backend
- HTML5 Canvas frontend
- WebSocket real-time communication
- Grid-based architecture for LED panels

Version Numbering:
- Major releases: 1.0, 2.0, 3.0
- Major incremental: 1.1, 1.2, 1.3
- Minor fixes: 1.1.1, 1.1.2, 1.1.3
